# Joint 在线联合决策与软硬协同实验方案

日期：2026-09-21。

> 本文档是机制实现、仿真与实验排期的内部依据，不应直接照搬进论文。研究范围涵盖 Joint 联合决策、FACE/WSC-LLM 的多轮 Agent 适配对照及相关工作边界。文献事实以原始论文为准；未核实的会议归属不得写入主论文。

> **研究与执行入口：** 全文统一采用 joint：同一硬件配置内 instance 同构，全部 instance 都保留在初始映射候选中；request 从队列发射时联合选择执行位置和 KV 动作，并按实际空间压力动态驱逐。初始 argmin 不用 HBM 容量可行集预过滤，HBM 容量进入逐 rank 的空间/驱逐代价；选中后仍由 reserve/prepare 做真实容量检查，暂时缺口回队等待，终态不可行按结构性失败记录。代价只使用硬件配置、运行时负载和已知历史，不依赖离线 workload 调参。第七节给出实现边界和实验流程；证据材料仅用于追溯，不能覆盖这些约定。本次文档更新不启动实验。

---

## 一、研究问题：跨轮计算位置与 KV 布局的联合优化

Agent session 的历史 KV 会跨请求持续存在。在具有二维 Mesh、空间分布的 HBM、边缘片外内存端口和共享 NoC 的晶圆级系统中，本轮 request 的执行位置会影响未来多轮的 KV 驻留、恢复、远程访问、排队和资源争用。这个问题具有明确的体系结构价值。

但按“两个朴素策略 + 统一片上内存域 + 性能比较”的叙事直接投稿，容易被视为已有机制的组合。以下内容不能单独作为核心 novelty：

- 晶圆级 KV 管理或跨 instance 的远程 HBM 访问；
- 由 D2D 带宽 / DRAM(HBM) 带宽决定的远程访问邻域；
- 历史 KV 亲和性与负载均衡的简单结合；
- 工具调用与人类输入的简单分类；

尤其是，FACE 的 Optimized Memory Management 已经支持跨 instance 存储和远程访问 KV，并以如下条件确定 decode 的可调度范围：

    Distance(ins_p, ins) <= D2D_Bandwidth / DRAM_Bandwidth

KV 空间不足时，FACE 还可将其分布到邻近实例。FACE 的 P 和 D 也可被独立映射，并不要求同一 request 的 P/D 共置。WSC-LLM 同样使用晶圆上的分布式 DRAM 存放 KV。因此，不能将“局部统一内存域”“邻近远程 KV 访问”或上述距离公式本身写成本文首次提出的机制；实现 baseline 时也不能遗漏这些原有能力。

论文更可检验、也更可能成立的命题应是：

> 在由同构 instance、分布式 HBM、二维 NoC 和边缘片外内存端口组成的晶圆级系统中，如何仅根据硬件配置、已知历史 KV 和实时负载，联合选择执行位置与原地复用、重算、复制或远程读取动作，并通过发射时动态驱逐适应不同硬件和负载？

instance 同构不表示访问代价相同：历史位置、到边缘端口的距离和当前争用仍有差异。研究目标是可跨配置运行的在线代价计算与联合决策，不是预先搜索每个 workload 的最优阈值。代价模型不把真实未来输出长度、未来 session 返回或完整 trace 暴露给调度器。

这里的贡献不是把已有能力“组合”起来，而是证明跨轮状态造成了已有 request-level 调度无法处理的代价结构，并提出能稳定改善该代价的联合决策。若强基线已经消除了主要收益，应据实收缩或重构主张。

保留 remote-read 为正式候选；实验用于确定其性能、完成率和适用范围，不作为是否保留的前置条件。copy 采用流式迁移与执行端留存，共用块管理和资源模型，不另设 copy 的局部统一内存域。远读域不能裁剪 copy 或其他动作的实例候选。

### 1. 唯一方法主体与受控对照

- **正文以 joint 为核心。** 当前运行时由工具/人类返回类型感知（T）、instance × KV 动作联合决策（J）和自适应层数逐出（E）协作：T 在合法 inactive victim 中按 human→tool 或类型无关 LRU 排序；E 在真实容量缺口出现时按层消费期限求 `k_hide`，由事件递推预测器驱动逐层/逐组释放；J 在全部 instance 上比较 stay、recompute、copy、remote-read。新 KV 在执行 instance 生成；copy 的图路径已按块建立就绪门和交接锚，remote-read 已接入 credit 分块读流与合并结算。当前边界是：E 已接入 face_scheduler 的实际驱逐规划，但 `JointCostModel.estimate_action` 的 `eviction_wait` 仍按逐 rank 空间缺口、可回收量和池写回估计，尚未把 `k_hide`、逐层恢复和 E 的暴露停顿直接纳入候选 cost；因此“运行时 E 已实现”不能表述成“统一代价已完整耦合 E”。copy/remote 的执行事件与 JCM 计价也需按下文边界披露。三者是协作模块，不预先包装成三个独立创新点；历史结果不能证明完整三机制已经在所有路径上验证。
- **主性能图默认以四个外部对照加最终方法为主要比较集合。** 外部对照是 FACE/WSC-LLM 各自的多轮 Resident 与 session-level Tiered-LRU 适配版；后者是为公平支持多轮 Agent session 而作的本文 baseline 扩展，不是原论文已有行为，也不构成本工作的贡献。总览图可因预先定义的研究问题增列最强简单策略，但不能按事后输赢删选；四外部比较集合仍保留，不要求每一张图都排成五柱。
- **策略替代与机制消融分开。** recompute-only、history-affinity-only、load-only 是同一实现上的内部对照；remote on/off 是能力消融；同状态四动作比较用于验证代价和解释选择。它们回答不同问题，不能互相替代。
- **既有证据只按边界复用。** phase1/phase2 和 predictor 结果只能作输入、回归或历史背景，不能证明最终局域域。full 等既有结果按其完成覆盖和实现范围使用，不作为当前候选的受控配对证据。

---

## 二、相关工作边界与必须处理的近邻方法

### 1. FACE、WSC-LLM 与本文的真正差异

FACE 和 WSC-LLM 的重点是单 request 的服务与 P/D 调度；它们并未建立跨轮 session 的长期生命周期管理目标。本文若要成立，应把差异落在以下可测量问题上：历史 KV 的跨轮可复用性、等待期间的驻留/驱逐、恢复准备、执行位置，以及由此引起的源 HBM、NoC 和边缘端口争用。不能把 baseline 中已有的远程 KV 路径所带来的收益计入本文。

### 2. Mooncake：可引用为“传输—重算权衡”，但不是本文远程读—迁移判据的先例

Mooncake FAST 2025 正式版 §2.2 的式（2）确实给出与带宽和计算能力相关的前缀复用条件。但它比较的是：

1. 将命中的前缀 KV 从远端 CPU 内存一次性载入 prefill GPU HBM，然后进行 incremental prefill；
2. 不复用该前缀 KV，重新计算相应 prefill。

它不比较“逐 decode step 的远程 KV 读取”与“一次复制后持续本地读取”，公式中也没有 Mesh hop、未来访问次数或多流竞争。因此可将其作为“KV 传输与避免重算之间存在条件性权衡”的相关工作，但不能声称它已解决本文的远程读—迁移分界。本文必须处理访问次数的不确定性、KV 大小、路径、源 HBM 和链路竞争；真实剩余 decode 长度不能作为在线输入。

### 3. 工具感知、会话预测和分层缓存工作

- Continuum/CacheTTL 一类工作已直接研究工具调用期间 KV 的 TTL/pinning，并将返回后的排队代价纳入考虑。若本文使用工具/人类类别或返回时间来指导保留，必须将其视为近邻基线或清晰说明其局限；简单的“工具 session 更值得保留”不能独立构成贡献。
- LPC 一类工作已使用对话继续概率改进基于 recency 的驱逐。因此，“预测未来复用优于 LRU”也不能单独主张为新意；本文需说明预测信息、空间约束和优化目标的不同。
- CachedAttention（ATC 2024）中的 AttentionStore 已涉及分层 KV 保存、预加载和异步 I/O。若采用层级保留/恢复，须用正确依赖关系解释本文与其差异；该工作不能被简化为缓存比例启发式。
- KVFlow 与 Agent 工作流有关，但其公开版本和具体发表 venue 必须以原始记录核实；在确认前只将其称为 arXiv 工作，不写作“NeurIPS 2025”。

### 4. vLLM/SGLang 与 session-level Tiered-LRU 的边界

vLLM Automatic Prefix Caching（APC）/PagedAttention 与 SGLang RadixAttention 可作为生产级前缀复用和 LRU 置换的参考，但其 token block 分配、哈希前缀、引用计数、活跃请求抢占和缓存驱逐是彼此不同的机制。将冷 KV 写回片外池、恢复跨轮 session 历史，是针对本系统的适配，不能表述为“vLLM 默认已有的分层 session 冷存储”。

因此，本工作的通用强对照统一称为 session-level Tiered-LRU。若实际实现或引用 vLLM 行为，须固定具体版本/commit，并说明使用的是哪一项机制。

---

## 三、需要建立的技术论证

### 1. 不预设唯一的决策顺序：采用“目标实例 × 动作”联合决策

`joint` 在同一队列发射决策点比较“目标 instance × KV 动作”，不先按负载锁死实例、再按 hop 选动作。每个候选从同一只读状态快照估计计算、等待、数据访问和驱逐代价；比较候选本身不能修改驻留状态。

候选表示为：

    S_exec = 当前硬件配置中的全部 instance
    (目标 instance, stay / recompute / copy / remote-read)

所有 instance 始终保留在初始映射候选集中，不按剩余 HBM、距离、边缘位置或历史亲和做预过滤。动作语义仍有区别：stay 只适用于相应历史在目标本地可用的情形；remote-read 需要真实可达的 direct-access 路径。某个动作不适用不等于删除该 instance，其他动作仍参与比较。`R_direct` 只描述直接远读能力，不能替代 `S_exec`。

片外恢复是 copy/恢复路径的显式数据来源，不得冒充片上 remote-read；缺失历史的重算计入 recompute。目标需要腾出空间时，JCM 先将逐 rank 缺口换算为驱逐/写回等待，选定动作后才提交实际驱逐和数据移动。实际提交仍由 KVCacheManager 检查每个 rank 的容量、预约和合法 victim；容量不足会回队等待或在全部组合不可恢复时记录结构性失败。全候选假设不意味着无限内存或免费释放正在使用的 KV。

近处排队、远处空闲、源端前驱阻塞和共享 NoC/HBM 干扰都可能改变动作优劣。负载量必须通过有单位的服务模型换算成时间，不能把队列长度或 primary workload 差直接当作节省的等待时间。代价计算细则见第 7.2 节。

### 2. 远程读取、迁移和原地执行的分界条件

copy 的运行时路径按消费顺序切成 NoC 前缀块和池后缀恢复组；图构建器为块/组建立目标 HBM 完成门，计算按所需数据就绪消费，交接 journal/ack anchor 记录源块安全释放条件。当前 face_scheduler 的 KV 账本在 prefill_drain（merge 为兜底）批量结算已完成交接块，因此“目标完成后立即释放”在图依赖上有锚点、在管理器占用账上仍以 drain 为更新边界；JCM 已将前缀按同一 8 层块规划拆成首块就绪和尾流，并用 `max(尾流, compute)` 计入关键路径，池后缀仍与首块处于准备阶段。源端提前释放对后续请求的外部性仍需 raw 事件核验。轮末只完成执行端会话元数据结算，不重复释放已结算源块。

remote-read 运行时保持基础历史在源端有效，执行端只建立必要暂存并按 credit 分块消费；适用面为 LOCAL 基（全层前缀读流）与 PARTIAL 基（缺失后缀在准入相池恢复物化＋驻留前缀读流），REMOTE 基（历史整体在片外池）不适用 remote-read，该情形整体走池恢复；源端有效读取期间受保护，本轮增量和 merge 结算另计。JCM 已有 remote first-credit + max(remaining-stream, compute) 的统一估价，并按源 HBM/逐跳 NoC/目标暂存计费。JCM 的远读字节估价必须与执行口径一致：运行时每个 decode 步读取的是基础历史＋输入＋已生成内容的终态上下文（均匀口径），JCM 不得只按基础历史×步数计价；decode 腿按同一终态上下文口径以闭式计算读量（预填充腿维持基础历史单遍口径，两侧一致），步数输入为在线 decode 步数估计，不使用未来输出长度；credit 块 K 的 steps 输入两侧取同语义（decode 步数），多列车首块流水重付按实测量级披露；修订后以同状态事件回放核验预测误差。

两者使用共同的传输、块位置、缓冲和资源计价机制。copy 不单独建立局部统一内存域；远读供给条件不满足的目标仍可能因后续本地访问或计算负载优势成为合算的迁移目标。

在单 shard 基础历史为 c、源端与本地参考 HBM 读带宽相同且无其他争用、目标暂存非瓶颈、稳定流水并忽略启动/同步时，历史扫描的简化参考为：

\[
t_{\mathrm{local,hist}}=c/B_{\mathrm{HBM}},\quad
 t_{\mathrm{remote,hist}}\approx\max(c/B_{\mathrm{HBM}},cd/B_{\mathrm{D2D}}).
\]

`d ≤ ρ = B_D2D/B_HBM` 只表示该简化历史读取项可与本地参考相同，不保证完整请求平价。实际供给由源 HBM、逐跳链路、目标 HBM/暂存及传输通道的共享份额共同决定；启动、同步、容量准备和合并另按依赖计入。`ρ < 1` 表示上述峰值参考的带宽平价条件不成立，不能推出远读没有系统收益。

copy 的首次读流只在真实相同的部分与 remote 对消，不能无条件对消整个预填充阶段。执行端留存写入即使可以重叠，也可能成为瓶颈；完整请求优劣由统一时域下的关键路径决定，不能由 d 与 ρ 单独裁决。

对本次迁移的基础历史 H，容量账应满足：

\[
H_{\mathrm{home}}(t)+H_{\mathrm{exec}}(t)=H+D_{\mathrm{handoff}}(t),
\]

其中 D 为真实交接过程中的重复有效载荷。增量、缓冲、工作区和其他请求另计；实际占用与未物化预留分开记录。正常无重传迁移中，原有历史有效载荷搬运一次，不代表驱逐、恢复、逐跳传输和全部通信只有一次。

目标端容量压力可能增加 remote 的相对价值；源 HBM 和远读路径竞争通常增加其持续成本。copy 驱逐量按实际空间缺口及合法释放计划求得，不固定为 H。源端释放由真实状态更新体现，不直接抵扣目标端等待；忽略受害会话恢复或源端空间利用的净偏差方向不能预设。

上述解析关系用于诊断和解释，不是读取真实剩余长度的规则。硬件数值逐配置派生，在线时域对全部动作一致且无未来信息；完整在线对照同时观察焦点请求、背景请求与后续轮次。

### 3. 动态驱逐与数据就绪

E 已接入运行时的 `adaptive` 层级驱逐：保留连续前 `k` 层，在计算热层时按消费顺序恢复冷层。目标是预测能隐藏恢复的最小热前缀，而非固定半层；它服务于真实容量缺口的释放计划，不是发射前的 instance 可行性掩码：

\[
\widehat k_{\mathrm{hide}}=\min\{k:\widehat R_\ell(k)\le\widehat D_\ell(k),\ \forall\ell>k\}.
\]

`R` 为冷层恢复完成时刻，`D` 为无冷 KV 阻塞时的层消费期限。串行恢复简化式为 `R_l=q+sum(r_j,j=k+1..l)`、`D_l=sum(c_j,j=1..l-1)`；正式实现用共享资源事件递推处理多流、缓冲与计算互扰，检查所有冷层。目标是软保留量；实际缺口可迫使进一步逐出，分别记录目标/实际层数及暴露恢复延迟。没有空间需求不主动搬运。运行时 `KVCacheManager._ensure_capacity` 在逐 rank 缺口出现后调用层策略和 `LayerRecursionPredictor`，无样本/预测异常时保守保留全部层；无合法 victim 或仍不足则回队重试/记录结构性失败。该 E 逻辑已参与实际驱逐，但 `JointCostModel.estimate_action` 的 `eviction_wait` 还未调用 `k_hide` 预览或事件递推停顿，候选 cost 只估逐 rank 缺口、可回收量和池写回。

参数设置：模型层数/布局、峰值算力、带宽/延迟来自硬件配置；缺失字节来自已生成 KV 的区间账本；恢复时间由候选加入后的因果可预测仲裁份额、在途剩余字节和依赖预测，不能用 `带宽×(1−利用率)` 替代。prefill 工作量使用已到达输入及已知历史，服务效率只由已完成事件因果更新，剔除排队/冷 KV 等待并避免重复计费。无样本用硬件模型冷启动；估计规则跨 scan 点固定，估计在每个决策点现场计算、无跨点缓存，每个独立实验点从相同定义的冷启动初态开始、不继承先前实验点的样本，不离线拟合每点阈值。

严格禁止读取 CSV 未来请求、未来输出/返回时间或仿真器预存的未来真值。等待 session 的下一轮输入仅用已完成轮次的在线统计估计，无样本保守保留目标设为全部层；参考当前位置/负载的估计不是未来保证，返回后重算。公式与更新规则先冻结；小实验用于验证误差和收益，不用于获取 workload 调参表。详细参数、在线更新、接口及因果审计见仓库设计文档第 5 节：

`@paper_write/本论文撰写内部材料/三机制联合策略_template仓库设计方案.md`

验证包含均匀层解析例、长历史短输入、带宽/真实竞争升降、未来输入扰动隔离及逐层真实恢复/计算重叠；对比 session-level Tiered-LRU 的固定整会话规则和最终方法的自适应层数，输出到位/消费误差、实际阻塞、保留字节和总时延。数据未就绪必须真实等待；恢复无等待气泡不等于没有共享资源干扰。

当前代码已固定上述规则：victim 必须来自 completed/inactive 会话，T 给出 human/tool 或 LRU 顺序，E 给出层/组释放计划，`KVCacheManager` 按逐 rank 缺口提交池写回；无合法 victim 或仍不足时抛容量类错误，由在线准入回队重试或记录结构性失败。图侧保护数据就绪和 source ack；管理器账本的 chunk release 仍在 prefill drain 结算，E 与 JCM cost 也仍需独立对照。后续实验验证这些边界，不把尚未耦合的 E 暴露费用写入统一 cost。

### 4. 跨轮 trace 回放必须保持因果关系与预测误差

对 session s 的第 k+1 轮请求，到达时刻应由本策略下上一轮的完成时刻决定：

    a(s, k+1) = f(s, k) + z(s, k)

其中 f(s,k) 为本策略下上一轮向外完成响应的时刻，z(s,k) 是工具或用户等待时间。到达后仍须满足真实块就绪和合并依赖，不能把响应结束等同于资源已经结算。固定重放绝对到达时间会使慢策略产生“前一轮未完成、后一轮已到达”的不真实依赖，污染驻留和排队结论。

trace 改造应保留或报告 session 内轮次、工具/用户类别、等待时间、历史增长、输入/输出长度和并发关系。后端可用真实长度生成工作量及终止事件，但不能向 joint 泄露未来输出、未来返回时间或下一轮复用。若使用在线估计，只能依赖当前已知信息及过去已发生的事件，须报告冷启动、估计误差和更新规则；不要求额外训练一个离线预测器。

---

## 四、外部 Baseline：资源匹配、能力忠实、归因明确

### 1. 主图的四个外部对照

主性能图默认以最终方法对照以下四个外部配置；总览图可因预先定义的研究问题增列最强简单策略，但不按事后输赢挑选或删除它。FACE/WSC-LLM 保留各自原有的 P/D 组织、调度目标、片上 KV 分配和远程访问能力。Resident 与 session-level Tiered-LRU 都是本文为多轮 Agent session 构造的资源匹配适配，不能归因给原论文。

FACE 和 WSC-LLM 均应保留两个面向多轮 session 的适配版本：

| Baseline | 历史 KV 管理方式 | 角色 |
| --- | --- | --- |
| WSC-LLM-Resident | 片上保留历史 KV；容量不足时按 LRU 删除；之后缺失部分重算 | 参考：量化片上保留相对重算的收益 |
| FACE-Resident | 同上；保留 FACE 原有请求调度和片上 KV 能力 | 参考 |
| WSC-LLM-session-level Tiered-LRU | 非活跃 logical session 按 LRU 整体写入片外池；按需整体恢复 | 主要资源匹配强基线 |
| FACE-session-level Tiered-LRU | 同上；保留 FACE 原有请求调度和片上 KV 能力 | 主要资源匹配强基线 |

session-level Tiered-LRU 是检验“在已有冷存储之后，本文方法是否仍有收益”的主对照；Resident 是解释“保存冷 KV 与删除后重算”的必要受控对照。四个外部配置构成默认主比较集合；若版面、研究问题或图的归因目的要求分图或增列预先指定的简单策略，不得因此改变集合、事后删选最强简单策略，或把未展示的 Resident 解释为不存在。

配置名称必须按实际实现映射到仓库 run；不能仅凭名称把已有的重算结果重新标为 Resident，也不能在本轮文档更新中宣称已经实现这些多轮适配。既有历史记录保持原样。

### 2. 公平实现规则

1. 保留 FACE/WSC-LLM 原有的 P/D 组织、调度目标和片上 KV 分配/远程访问能力。FACE 中 P/D 可以独立选择；不得为便于比较而强制同 request 共置。
2. baseline 中仍在其他 instance 的历史 KV，必须仍可按原论文能力被远程访问或移动；不能把它错误地当作片外缺失或重算 miss。
3. 明确称其为“为多轮 Agent session 适配的 FACE/WSC-LLM 变体”；不要把跨轮保存、LRU 或片外冷热管理说成原论文已有行为。
4. Resident 与 session-level Tiered-LRU 在受控对比中使用相同 HBM 容量、水位线和逐出候选集合；差别仅为逐出后删除重算，或将完整 logical session 写入片外池并按需整体恢复。比较各方法各自最优性能时，给予相同硬件资源和调参预算，而非人为压低 baseline。
5. session-level Tiered-LRU 采用最近一次实际 KV 使用时间的非活跃 logical-session LRU；不引入预测器、动态层数、半层比例或 layer/block 级逐出。
6. 完整计入 HBM 读写、NoC、源 HBM、边缘端口 FIFO、片外带宽、恢复/迁移路径及与 TP 通信的竞争；换入换出不能视为零代价。

### 3. 本文内部：统一实现上的策略替代与能力消融

内部对照使用 joint 的共同实现、全部 instance 候选、发射时驱逐及真实事件模型；不使用容量可行集过滤。

- **recompute-only：** 不复用跨轮历史 KV，按相同模型重新计算必需历史。执行位置使用共同的负载排序；必须计入全部重算工作，不删除 trace 中的历史语义。这是复用能力边界对照，不声称只改变了一个数值参数。
- **history-affinity-only：** 有可复用历史时优先选择其 home；无历史或历史布局没有唯一 home 时使用预先定义的共同回退。候选集仍是全部 instance，亲和是选择偏好，不是容量/距离掩码。选定实例后的动作计费、驱逐和生命周期保持共同规则。
- **load-only：** 忽略历史位置对选点的偏好，在全部 instance 中按在线负载估计选择；选定实例后的 KV 动作仍真实执行并计费。与 history-affinity-only 的差别是位置选择规则，不能悄悄关闭其通信或历史访问成本。
- **joint：** 同时比较实例和动作，而非先固定执行位置。另做 joint 的 remote on/off 能力消融：只改变 remote 动作的可用性，保留全部实例和 stay/recompute/copy 路径，不把整个调度器替换为另一方法。

上述策略替代固定 P/D 组织、模型/trace、初始状态、首轮处理、驱逐规则和数据粒度、硬件资源及在线估计更新规则；下面的 T/J/E 消融仅改变指定模块。P0 记录每个开关具体改变的分支，以及无历史、部分历史和分布式历史的共同处理；PARTIAL 状态不形成实例掩码。对照中偏好 home 不改变 joint 的全候选设计。外部 baseline 保留各自原有 P/D 与 KV 能力，不受内部策略组织方式的强制限制。

#### 三机制消融与外部系统比较的分工

仓库实现边界、自动合并的两处容量压力及 E/JCM 耦合边界见：

`@paper_write/本论文撰写内部材料/三机制联合策略_template仓库设计方案.md`

**整体性能比较**由完整 T＋J＋E 对照 FACE/WSC-LLM 的 Resident 与 session-level Tiered-LRU 四个适配版，回答“完整系统相比已有方法有什么收益”。报告统一口径的 E2E、尾延迟、SLO、完成率，以及解释差异所需的迁移、恢复、remote 流量和驱逐成本。未激活片外路径的点不能证明冷热管理没有价值；未真实复现指定版本的 vLLM 策略，不把 session-level Tiered-LRU 笼统称为“vLLM 版”。

**机制消融**全部使用本文共同实现，回答“收益来自哪个模块及其交互”。FACE/WSC-LLM 及其变体不参与八组合，也不能替代“三无”基础组，否则其 P/D、KV 布局和执行差异会混入机制归因。关闭模块使用以下合理替代，不删除基本服务能力：

| 开关 | 关闭后的共同替代 | 要隔离的因素 |
| --- | --- | --- |
| 无 T | 关闭人类优先/工具后置顺序，使用同一合法 victim 集合上的类型无关 LRU；不改变层数策略及保护规则 | 类别逐出优先级本身，而非有无历史复用或预测器 |
| 无 J | 按固定负载或历史亲和规则先选 instance，再在该位置选择 KV 动作；保留 T/E 和同等动作能力 | 联合选择是否优于顺序选择 |
| 无 E | 使用 session-level Tiered-LRU 的固定 session LRU、整体片外写回和整体恢复 | 自适应层数决策；其他 T/J、生命周期与资源计费保持一致 |

无 E 使用 session-level Tiered-LRU 的固定规则；T 使用的下一轮交互类型由本轮已完成响应揭示（响应含工具调用则下一轮为工具返回，否则为人类输入），在完成时刻即因果可得，其实现形式（trace 物化列、完成时写入 session 状态）与该因果语义一致，边界约定（session 末行归 human、零间隔后继行归 tool）须在论文写明；T 不使用未来返回的真实时间。主消融固定 KV 物理布局和新 KV 写回/合并语义，布局变化另做设计比较。

| 核心比较 | 用来说明的问题 |
| --- | --- |
| 完整 T＋J＋E vs J＋E | 类型信息在完整系统中是否有额外价值 |
| 完整 T＋J＋E vs T＋E | 相同信息、逐出和动作能力下，joint 是否仍有增量；这是突出核心创新的重点 |
| 完整 T＋J＋E vs T＋J | 自适应逐出是否优于合理的按需逐出 |

无 J 的关键比较分别采用负载优先、历史亲和优先两个顺序参照，不能只选较弱的一种。少量代表性点再补齐“无 T/J/E、T、J、E、T＋J、T＋E、J＋E、T＋J＋E”八组合；其中无 J 使用同一个预先固定的顺序参照。四个核心组解释完整系统中的边际贡献，八组合进一步解释模块单独作用和交互，不预设融合必然最好。

**扫描与机制解释**：完整方法运行预先登记的硬件 × 负载矩阵，关键消融和外部基线按研究问题选择比较点；八组合优先覆盖低竞争、明显计算/通信竞争和真实驱逐压力，不对所有点机械展开。T 关注返回后驻留/恢复及两类请求的得失；E 关注实际与所需释放字节、写回/恢复、反复逐出及等待；J 关注选点/动作、queue、链路竞争及 E2E/P95。各点冻结规则，硬件派生参数与在线状态正常更新，不按结果调参。

joint 内部另做 remote on/off、硬件派生服务率/在线修正服务率、简化/聚合链路争用模型等有针对性的对照；其余 T/E 和能力保持一致。流量治理须匹配：分别比较 remote-on/off 无配额、remote-on 有/无配额，以及 remote-on/off 有同等通用治理。远读专用配额须注明范围，不能将通用拥塞控制的收益归因给 remote 动作。remote-off 是受限动作参照，不是性能上界；同状态四动作误差与排序验证属于代价模型验证，不替代服务级消融。

若 T/E 的贡献很大且能自然移植，可在代表性点增加“FACE/WSC-LLM 冷热管理版＋相应支撑机制”的增强对照，检验已有系统补上这些能力后是否仍需 joint。它是条件性的补充归因实验，不是所有外部基线都必须加入 T/E，也不替代第四节第 4 项的近邻方法比较。以上只补充实验思路，不授权新仿真；现有执行状态不等于三机制消融已经完成。

### 4. 近邻策略对照

除通用 LRU 外，若本文的主张覆盖相应功能，应加入至少一个最接近的策略对照：

- 若主张工具返回信息带来收益：实现或适配 Continuum/TTL 式工具感知保留，并在同一资源模型下比较；
- 若主张逐层恢复/预取：与可实现的 CachedAttention 风格层级保存/预加载思想做特征匹配对照；
- 若主张未来复用预测：与预测驱动的驻留/驱逐策略做对照；
- 若主张历史亲和与负载均衡：与 Preble 式 cache-aware routing 的核心原则进行对照或明确差异。

不需要机械地把所有生产系统的全实验矩阵移植进晶圆模拟器；应预先按核心 claim 选择最接近的一项，忠实实现其可比决策，并在论文中说明不能直接移植的系统假设。

---

## 五、实验问题、指标与受控归因

每组实验对应一个研究问题，执行顺序见第七节。硬件/负载 scan 检验同一在线决策规则的适应性，不为各扫描点搜索权重或要求 joint 必须胜出。本文档不直接启动实验；获得执行任务后，按预先登记的有限矩阵连续交付，不在普通负结果后逐项请示。

| 组别与问题 | 改变项 | 固定项 | 输出 | 复用边界 |
| --- | --- | --- | --- | --- |
| 单动作模型：预测是否可信？ | 同状态下的 stay/recompute/copy/remote-read | 决策锚点、预测时域、初态、已知历史、背景工作与硬件 | 时间误差、排序错误、选择损失、实际字节与事件 | 未选动作真值来自独立同状态受控执行，不能从一次在线运行生成 |
| 在线适应：负载改变时能否合理更新？ | 真实背景计算/通信和到达强度 | 计算公式、更新规则、硬件、trace 逻辑及种子 | 参数时间序列、选择/切换、误差、queue 与背景延迟 | 不人为改 queue 数字而不执行对应工作，不强求必定切换 |
| 软硬协同：硬件变化如何改变动作收益？ | 硬件配置 × 负载强度 | 算法公式、在线更新规则、输入生成规则 | 尾延迟/SLO、驱逐/重算/通信、动作分布与配置成本 | 每个配置内部同构；区分单因素敏感性与多因素配置比较 |
| 外部主对照：是否优于资源匹配的方法？ | 四个 FACE/WSC-LLM 适配配置与完整 T＋J＋E | 同硬件、trace、初态；各自原有 P/D 与等资源预算 | E2E/SLO、完成覆盖率、流量和驱逐成本 | 保留原有能力；分层路径未激活时不能声称验证了分层收益 |
| Resident→session-level Tiered-LRU 归因 | 删除重算或片外保存/恢复 | 同一外部调度、容量、逐出集合与资源 | 实际恢复/重算量、延迟与 SLO | 这是本文多轮适配，不是原论文已有行为 |
| 内部策略替代 | joint 与三个内部对照 | 第四节定义的共同实现与资源 | 配对 E2E/SLO、完成覆盖、选点/动作及资源 | 不复用历史程序的结果冒充单因素消融 |
| 三机制消融：各模块及交互是否有效？ | T/J/E 核心四组，代表性点补齐八组合 | 共同实现、KV 布局、物理能力及非被测规则 | 边际贡献、交互、释放/恢复与调度行为 | 三无使用共同实现的基础规则，不由外部 baseline 替代 |
| 动态层数：最小热前缀预测是否有效？ | session-level Tiered-LRU / 自适应层数；历史、输入长度及真实带宽竞争 | victim 规则、层级执行、因果观测和更新公式 | 目标/实际层数、逐层就绪误差、暴露恢复时间、占用与总时延 | 不预读未来 CSV；先验证再扫描，不以无气泡代替无性能代价 |
| remote 能力增量 | joint 的 remote on/off，并匹配通用流量治理 | 全部 instance、其他动作、硬件、驱逐、初态及更新规则 | 焦点/背景性能、完成率、remote/copy 字节、资源占用与准入等待 | 动作缺失不删 instance；通用治理下 copy/恢复获得同等能力 |

服务指标保留 alpha=6、epsilon=0.01 的 server-side E2E-SLO 评估；参考、分母及覆盖要求见第 7.4 节。报告均值、P50/P95/P99、session 完成时间、累计排队，以及实际观测支持的吞吐/goodput。首次到达压缩或有限 episode 的吞吐不等于稳态 SLO 容量，不强制开展容量边界搜索。TTFT 无真实 token 事件时以打标 proxy 或 NA 披露，proxy 禁止进入 SLO 判定；TPOT 无真实逐 token 事件支撑，不列入报告指标。KV 命中率、重算 token、暴露恢复延迟、HBM 占用、链路/端口争用和动作字节只能按实际观测报告。

策略替代与单组件/动作能力消融均须实际运行；正文方法与实验臂按本合同定义。always-copy 和 always-remote 可作为局域动作的边界策略，帮助解释适用区间；它们不是严格的性能上下界。无限带宽或完美预测同样应明确标注为诊断 oracle，而非联合调度的理论最优。

---

## 六、当前执行边界与 Go/No-Go 门槛

### 当前应保留的比较纪律

1. FACE/WSC-LLM 的原始 P/D 组织、分布式 KV 与远程访问能力仍须忠实保留；多轮 Resident 与 session-level Tiered-LRU 适配版本仍是资源匹配和归因所需的 baseline。
2. Resident 与 session-level Tiered-LRU 的冷热管理比较仍应保持相同 HBM 容量、水位线和逐出候选集合；冷 KV 保存、删除重算和片外恢复的代价必须完整计入。
3. 历史固定亲和结果仅作回归背景，不替代完整局域域、remote 或 copy 的物理机制验证。
4. 策略替代和 remote on/off 严格使用第四节定义；共同固定的是公式、更新规则和物理条件，不是在线负载或硬件派生参数的数值。消融不得重新引入 LOCAL/HBM 可行集筛选。
5. 既有 full 与 TP4/TP6 原始证据保持可追溯，按完成覆盖和配置差异使用；当前方法的配对比较必须满足同语义与同资源要求。

### 验证顺序与继续条件

1. P0 的四动作、动态驱逐、在线输入来源和事件接口已在 `joint` 运行时接通：图侧具备 copy chunk/restore-group 就绪门、remote credit 分块读流，`face_scheduler` 具备 T/E 驱逐和容量重试。当前 P0 级验证应核对交接锚点与 drain 结算、remote payload/ownership、账本守恒及同状态事件；JCM 已有 copy 首块/尾流关键路径，但源端释放外部性审计、E 暴露费用测量和 remote 闭式估价的同状态核验仍需完成。正式比较须使用同一套已核验物理语义。
2. P1 做同状态动作测量；P2/P3 检查真实动态负载与在线 remote 集成；P4 检查硬件参数派生；P5 汇总各阶段持续进行的正确性审计。
3. remote 在线路径、数据就绪和生命周期正确后，进入 P6 的有限硬件/负载扫描、内部对照与外部公平比较。代价预测误差、排序和收益均如实报告，不能为了使某动作胜出反复调参。
4. 以真实动作、数据就绪和守恒作为前置，不设置 HBM admission 或容量候选 REJECT。运行时驱逐不得免费删除在用数据；若硬件/工作负载超出模型能表达的范围，记录适用范围问题，不暗中缩小候选集。
5. 收益弱、不切换、SLO 不达或指标覆盖不足不单独构成停门。仅有真实接口/物理错误、无法确定的研究语义或预算边界阻断相应任务，其余独立工作继续并统一交付。

### Go/No-Go 判断

- 若 session-level Tiered-LRU 已消除大部分可解释收益，应收缩“冷热管理”主张，而不是把多个历史启发式叠加写成新颖机制。
- 同状态实验用于验证代价与解释适用区间，不要求 remote 在每种条件下获胜。其正式候选地位已经确定；若没有性能或完成率增量，如实保留结果并收窄论文主张，不能用挑输入或修改公平计费挽救叙事。
- 远读/复制若缺少真实 buffer、字节和事件语义，先修实现，暂不把该分支用于正式 scan；只有该实现阻塞，而不是因当前结果不理想阻塞整个研究。
- 最终要讲清的是 Agent 跨轮状态如何改变晶圆级计算、存储和通信的协同最优点，以及为什么已有 request-level 策略不能处理这种变化；不能只讲一套冷热缓存启发式。

> 具体完成状态、前置条件和接续任务以第七节为唯一台账。本节仅保留原则性 Go/No-Go 边界，不把计划表述成已执行事实。

---

## 七、Joint 实现与实验合同

本节规定实现语义、有限实验计划、指标及证据使用范围。相关源码、日志与报告只用于追溯和复核，不替代本文机制的当前验证。本次文档修改不启动仿真。

### 7.1 唯一现行方案与边界

- 当前方案名称为 `joint`。方法定义只有一套；版本标签仅用于标识实验记录的代码与语义，不构成并列方案。
- 同一硬件配置内 instance 同构，所有 instance 都属于 `S_exec`。不同 scan 配置可改变 instance 形状、数量、带宽和存储资源，但不能在单个配置中暗设不同容量等级。`R_direct` 只约束 direct-remote 动作，`S_exec` 始终是全部 instance；禁止容量、边缘位置、距离或 PARTIAL 状态形成隐含映射掩码。
- request 从队列发射时，由 `joint` 联合决定目标 instance 与 stay、recompute、copy、remote-read。容量只影响实际驱逐、等待和数据准备成本，不做 HBM 容量准入审查或候选 REJECT。映射选择与数据就绪是不同事件：选中 instance 后仍可等待合法驱逐/恢复完成，不能未就绪就进行消费。
- `joint` 每次实时计算四个动作的代价。输入只能来自硬件静态配置（拓扑、带宽、延迟、HBM 容量、端口/通信槽）、运行时状态（queue、active、inflight、实际占用、驱逐状态）和已知历史 KV。禁止提前 workload 统计、按工作负载搜索权重、使用未来输出长度 oracle。
- 固定安全常数可以保留，但必须给出物理意义、单位、来源和冻结方式；不得用安全常数暗含未来信息或变成按 workload 调参。
- remote-read 保留为正式在线候选；`remote off` 只作为能力控制组。实验确定收益和适用范围，不设置最低选中率，不用离线字节代理替代真实远读。copy 是流式迁移与留存，不另设 copy 域，也不受远读域候选过滤。

**发射与驱逐的共同流程：** 读取同一时刻状态快照 → 为全部 instance 估计适用动作及其驱逐/恢复工作 → 联合选点选动作 → 提交实际驱逐与数据准备 → 数据流首块就绪后 issue 计算（流式动作逐块就绪门控逐块消费）→ 按真实完成/释放事件更新状态。候选评估只生成预测计划，不能为每个候选实际驱逐。并发提交若使快照失效，按同一规则刷新状态，不引入容量候选过滤。

运行时已经固定驱逐粒度、victim 排序、在用/在途 KV 保护、删除重算与片外写回条件，以及无立即可驱逐对象时的等待/唤醒路径：正在消费的 KV 和 remote 未完成读取的源数据受保护，T/E 只在合法 completed/inactive victim 上工作。copy 图在目标 HBM 写完成处建立块级交接/ack anchor，`face_scheduler` 的占用账在 prefill drain（merge 兜底）批量释放已完成块；因此图依赖具备安全释放条件，但账本更新时间并非每个 chunk 的即时回调。remote 增量在执行端生成，按真实块集合和冻结的“少并多”规则结算，不能预设增量恒小于历史或 remote 恒保 home。以上是代码现状，仍需以同状态 raw 事件核验，不能把执行路径等同于预测/收益已验证。若单请求必需工作集超出执行模型的表达范围，记录配置/工作负载适用范围或接口阻塞，不通过无限缓存、静默丢请求或删除 instance 掩盖。

### 7.2 代价模型与可观测参数

在时刻 t 的只读状态快照下，对全部 instance 的适用动作预测到同一工作终点的完成时间。最小代价骨架为：

```text
cost(instance, action, t) = predicted_finish(instance, action, horizon | state(t), hardware) - t
choice = argmin cost(instance, action, t)
         over all instances and actions applicable to their KV/path state
```

当前 `JointCostModel.estimate_action` 已实现统一的 instance × 四动作 argmin 估价骨架：`target_wait + prep + effective_compute + merge_ns`，并按逐 rank 的 NoC/HBM/池路径计算准备时间。remote 的 `effective_compute` 已采用 `first_credit + max(remaining_stream, compute)`；copy 按运行时 8 层块拆分首块就绪和尾流，使用 `max(尾流, compute)`，池后缀与首块计入准备阶段，完整迁移量仍保留在审计字段。尚未完成的是完整验证：JCM 的 `eviction_wait` 按逐 rank 缺口、可回收量和池写回粗估，不调用运行时 `k_hide`/事件递推，也不直接计逐层恢复暴露停顿；该粗模型的预测误差与排序错误率须以同状态实验测量，仅在误差足以翻转结论时对进入关键路径的候选做 `k_hide` 懒式精化重估，不做全候选全耦合。源端 drain 结算的外部性只审计核账、不计入候选 cost；remote 闭式估价与实际读流的误差仍需同状态核对。

| 参数类别 | 来源与在线更新 | 在 scan 中保持不变的内容 |
| --- | --- | --- |
| 硬件基础量 | 配置给出的拓扑、路由、容量、带宽、启动延迟、端口/槽位及模型布局；每个配置重新派生 | 单位、派生公式和布局解释，不是派生数值 |
| 当前负载 | arrival/dispatch/issue/progress/completion 更新队列工作量、活跃任务、在途字节和链路/端口服务状态 | 状态定义与更新规则，不是负载值 |
| 计算/通信服务估计 | 已知工作量与硬件服务模型；可由当前运行已完成的真实事件修正有效速率 | 在线估计器、冷启动、统计窗口/遗忘规则；不能每个 scan 点事后重拟合 |
| 历史及驱逐成本 | 已知 KV 位置/字节/粒度、在用/在途引用、victim 计划及实际写回/重算成本 | 驱逐与保护规则，不是 victim 或其数量 |
| 安全常数 | 有物理意义的协议/调度常数，记录来源与单位 | 常数及其解释；禁止以 workload 验证结果搜索权重 |

**冻结的是公式、更新机制和少量有依据的常数；硬件派生值随配置变化，动态估计随事件实时变化。**“无需提前测试统计”允许使用已经发生的在线事件，不允许先用整份 workload 做 profiling、再给每个硬件/负载配置填最优系数。冷启动使用配置可推导的服务估计，并标记不确定性；缺少模型基础信息应登记缺项，不能暗中用被测 workload 拟合。各个独立实验点从相同定义的初态启动估计器，不能继承前一点的学习状态；单次负载升降实验则持续更新并记录这种连续性。

- **预测时域与终点。** P0 冻结对所有动作一致的合法预测时域，可用已知工作或过去事件形成的输出长度估计，并报告冷启动误差。`service_done` 为计算/响应完成，`merge_done` 为数据和会话结算完成，二者分别报告；若预测终点采用前者，后续合并义务仍须显式计价/约束，不能漏账。只在发射事务提交前、没有物理工作落账时允许撤销计划并重选；提交后本轮位置与动作冻结，P/D 同实例。预定 copy 的逐块迁移、交接和释放不属于重新选择动作。结算规则在实验前固定，轮末根据已经生成的实际 KV 和有效块位置执行“少并多”，相等保留量采用确定性规则；发射时只预测结算方向及资源义务，不能泄露未来输出。按规则更新 home 属于轮末结算，不属于轮内重选计算位置或动作。下一轮以真实位置和新鲜状态重新决策。

- **计算和排队。** 用已知 context、模型布局及当前调度状态估计工作量/服务率；队列长度、活跃数和 HBM 占用率不能直接作为秒数。仅可用过去已经暴露的 progress 估计剩余工作，不预读其他请求的最终长度。
- **remote。** 运行时按 `LOCAL/PARTIAL` 路径建立源 HBM 读取、逐跳链路共享、目标暂存写入/消费读取、credit 分块和 merge 结算（REMOTE 基不适用 remote-read，历史整体自片外池恢复）；已有源块在有效读取期内受保护。JCM 已计入源 HBM、逐跳 NoC、目标暂存和 remote first-credit/remaining-stream；decode 腿远读字节按与执行一致的终态上下文口径闭式估价（基础历史＋输入＋在线 decode 步数估计），预填充腿维持基础历史单遍口径，不复用运行时逐步 credit plan；实验需核对闭式估价与实际读流及同步/结算事件，K 的 steps 输入差异与多列车首块流水重付按实测量级披露。
- **copy。** 运行时按消费顺序切分 NoC 前缀块与池后缀恢复组，图构建器为块/组建立目标 HBM 完成门和交接 ack anchor，首块/首组就绪后计算可消费；目标留存写和消费读参与 HBM 竞争。源块的安全释放条件由 journal/anchor 记录，但 `face_scheduler` 的 KV 账本在 prefill drain（merge 兜底）批量结算，不能把它写成每块目标完成即刻更新账本。JCM 对前缀使用同一 8 层块规划，首块进入准备关键路径，尾流与计算取 max，池后缀仍计入首块准备；未完成交接的重复量、增量/缓冲、源端外部性和实际占用仍需按事件核账。
- **TP 与流水口径。** 按各 rank/shard 的实际字节、共享路径、端点及同步计算完成时间，不能用聚合字节除以单链路率，也不能无条件除以 TP 数。`first_credit + max(remaining_stream, compute)` 仅适用于对应稳定流水阶段，多列车、容量等待和填充/排空使用事件依赖。包含有效份额的服务时间不重复加同一竞争等待。

- **驱逐与复用。** 驱逐压力通过实际需等待/写回/重算的工作换算成时间，不把无单位的容量风险加到延迟上。复用收益优先体现为少做的重算或传输；已计入动作完成时间的收益不得再次相减。当前未知的未来返回次数不能凭空充当 history benefit。源端释放同样影响同期/后续请求，由真实状态演化体现；遗漏受害会话恢复与遗漏源端空间利用的净影响方向不能预设。若研究显式加入对其他请求的外部性，应另行定义对象、时域和防重复计费规则，不引入无解释权重。
- **代价审计。** 每次决策记录配置和状态时间戳、目标 instance、动作适用原因、时域、各项预测、选择/tie 原因、驱逐计划与实际提交事件。在线实测只属于实际执行动作；未执行动作真值填 NA，只有独立同状态受控执行才能提供其对照真值。backend 的真实调度事件是测量来源，不能用预测器自己生成的 cost 当作实测验证。
- **误差与决策质量。** 报告 MAE、分位误差、动作排序错误率；MAPE 对零值样本排除并单列数量，极短动作同时给绝对误差。在同状态对照中报告被选动作相对实测最优动作的时间损失；这只是受控状态内的选择损失，不是全局最优差距。性能和代价排序不符时解释原因，不通过手工调参消除负结果。

### 7.3 前期实验顺序（P0--P6）

P0 建立共同语义和最小动作实现；P1 验证单动作；P2/P3 验证动态状态和完整在线接入；P4 验证配置派生；P5 汇总从 P0 起持续执行的审计；其后进入 P6。阶段编号不是要求等到 P3 才开始实现 remote，或等到 P5 才检查守恒。例行实现/核账和普通负结果不需要逐阶段另行请示。

- **P0：语义、实现和计划。** 第 7.1 节的四动作、全部候选、驱逐流程、动作适用性以及第 7.2 节的 JCM 输入/时域已在当前 `joint` 运行时落地；remote credit、copy chunk/restore-group 图门控和 T/E 实际驱逐也已有代码路径。P0 仍需以 raw 事件核对源块交接/账本 drain 结算、块路由、remote ownership、守恒和同状态动作终点，并审计 JCM 的 copy 首块/尾流计价、remote 闭式估价字节与运行时实际读流、E 粗驱逐代价模型的暴露成本误差。离线重定价只给已有状态上的预测选择和差额，不提供替代动作真值或在线收益；不能把不存在的调度钩子或未来输出 oracle 当成模型输入。产出参数来源表、状态机、实现边界及有限 case 计划；代码审计不是 HBM 容量准入检查。
- **P1：四动作代价验证。** 从相同初态分别执行适用的四动作，至少区分无背景和有真实背景流量、短/长已知历史。记录源/目标、路由和实际数据消费过程，比较统一时域的预测与实测。全套 request 终态可以不同，但受控比较的工作终点必须一致。不做 workload 权重拟合；不能要求每个动作都获胜。
- **P2：动态负载响应。** 用真实背景请求/通信产生计算拥塞、链路拥塞和驱逐压力，并包含运行中的负载上升及下降。检查事件是否及时更新代价和选择；记录估计冷启动、更新滞后、切换/不切换原因和决策开销。只改 queue/占用计数的合成输入可作单测，不能作为真实性能证据。预测器不支持在线更新或出现反复切换时如实记录，不临时搜索平滑权重。
- **P3：remote 完整在线核验。** 运行时已有 remote 的真实选择、驱逐和后续执行路径；本阶段覆盖与 copy 竞争的相同背景，核对 payload、重复读取、源 HBM、路由/端口共享、staging、ownership、完成和释放，并核对运行时实际读流的字节/时间与 JCM 闭式估价的误差量级。至少取得一个真实 remote 执行路径；可以强制动作验证接口，但不能把强制执行宣称为 joint 自主选中。正式在线比较不要求 remote 自然选中率大于零，须同时报告零选中及其原因。自然出现的取消/失败须闭合，不要求复建容量拒绝实验链；未覆盖失败分支明确登记。
- **P4：硬件配置派生。** 以 template 的实际配置为锚，做有界的带宽、拓扑/instance 布局、容量及通信槽敏感性检查，避免所有维度的无意义笛卡尔积。同一配置内保持同构，容量变化只改变驱逐工作，不过滤 instance。复用同一算法和在线更新规则，硬件派生参数必须改变；不能把“无需调参”理解成不更新配置参数。TP4/TP6 同时改变多个系统量，单独标为配置对比。
- **P5：持续审计的汇总。** 汇总 P0--P4 的信息流、字节/事件、在用保护、数据就绪、ownership、释放及全请求终态审计；不是实验结束后补账。`reserve` 仅是生命周期准备，发起/消费所需资源和数据须在实际使用前就绪，释放只能在真实使用结束后发生。不能提前闭合尚未发生的完成事件，也不能用历史 PASS 代替当前 raw 证据。
- **P6：硬件 × 负载正式扫描。** 后续完整方案按第四节的 T/J/E 分工登记系统对照与消融：完整方法扫描、关键顺序策略/remote 对照，以及代表性点的三机制组合；既有运行不自动视为这些组合的证据。在合同可保真的点运行 FACE-Resident、WSC-LLM-Resident、FACE-session-level Tiered-LRU、WSC-LLM-session-level Tiered-LRU。所有方法在每个比较点使用相同硬件、输入和资源预算；外部原有 P/D/remote 能力不削弱。输出性能、代价误差、动作分布、动态驱逐与资源的联合解释，不只给平均加速比。不可比或片外路径未激活单列，不用更弱替代冒充强基线。

**扫描点生成。** P0 在看到该批性能结果前登记配置列表、负载生成规则、种子、初态、对照分支及 case 数。每个点能追溯到 template 或明确的物理参数变更。负载须区分首次到达压缩、session 闭环 thinktime、持续 session 到达和单次运行内的升降；只压缩首次到达不能声称持续饱和。优先用少量有物理依据的单因素检查确认实现，再做选定配置与低/中/高到达强度的有限交叉；强度标签不保证实际产生竞争，必须报告实测 queue/服务占用。不得依据初次结果再挑“joint 更好”的点或扩展未登记维度。

**当前实现与待验证边界。** 预测时域、在线速率估计与冷启动、victim 排序/驱逐粒度/等待事件以及 distributed/remote-backed history 的共同动作处理，运行时已有对应接口和路径：`event_recursion_predictor` 提供 adaptive 的因果恢复预测，`layer_eviction_policy`/`eviction_priority` 固定层组计划与 T 顺序，`face_scheduler` 负责逐 rank 容量提交和回队重试，JCM 已完成全部 instance × 四动作的统一 argmin 及基础传输估价，并已按 C13 规划计价 copy 首块/尾流。尚未闭合的是实验级 raw 证据、E 粗驱逐代价模型在容量压力下的预测误差与排序错误率测量（仅在误差足以翻转结论时对进入关键路径的候选做 `k_hide` 懒式精化重估，不做全候选全耦合）、copy 源端 drain 结算外部性的审计核账，以及 JCM 远读闭式估价的同状态校准。轮内冻结与 copy 块级交接遵循第 7.1—7.2 节，不另设重决策边界；新增算法假设需单独记录，不把上述待验证项写成已验证结论。

### 7.4 指标、证据与诚实边界

每个阶段必须保留 raw 事件、输入/配置 manifest、代码与参数 hash、失败 attempt 及复放信息。至少交付以下证据：

- 动作预测：MAE/MAPE、零值处理、误差分布、排序错误及同状态受控选择损失；未执行动作真值为 NA；
- 决策行为：切换点及不切换原因、目标 instance、chosen action、动态估计时间序列、更新延迟和计算/记录开销；
- 性能：E2E、P50、P95、P99，以及按 queue、compute、transfer、remote、contention、staging、eviction/recompute 等组成的 SLO 分解；
- 状态与流量：queue、active、inflight、各实例实际占用与未物化预留、交接重复量、evicted/recomputed/copied/remote-read bytes；copy 的目标写完成/位置交接/源端 ack anchor 与 drain 结算事件，remote 的 hop coverage、payload、有效带宽、ownership、completion、暂存与合并释放；
- 正确性：reserve/issue/write/consume/release 事件守恒、ledger 守恒、ownership 守恒、完成覆盖率和失败分类互斥闭合；
- 对照：消融及满足保真合同的外部 baseline 的同口径结果、不可比项和未运行原因。

服务评估沿用 alpha=6、epsilon=0.01；不将该评估目标混作 joint 的手工权重。每个硬件配置必须有对应的参考来源、单位、长度键和覆盖记录，不能套用 TP6 参考评估 TP4。现有 S3 是按新增 prefill/decode 的冷执行参考，不是等历史 isolated 基线；它可用于既定评估，但不能作为在线已知未来工作量，也不能据此把高违约率自动归咎于排队。缺参考且不影响在线决策时，对应 SLO 填 NA 并报告覆盖，不伪造参考或放宽 alpha/epsilon。

主服务分母包括所有预定请求；失败、超时、依赖取消等未成功服务不能从分母中消失。另报已发射请求口径、完成比例、参考覆盖以及完成集/共同完成集的条件延迟。分类互斥、缺失参考与失败分别记录，不能只用成功样本宣称全服务可行。E2E-queue 不能冒充隔离服务时间；hop 未认证时标 proxy/UNCERTIFIED，不据此声称完整 NoC 或能耗收益。字段和参考定义查阅以下指标文档；其中任何容量准入政策不作为 joint 的执行规则：

`@experiment/各指标测试建议.md`

`@experiment/SLO指标要求/SLO指标大全文档.md`

当前没有 held-out 证据。72、571、652 和 full 仍属于开发/回归资产；预先留出、未参与模型修订的硬件配置或负载强度可验证配置/强度迁移，但不等于独立 session/应用泛化。只有确实未参与设计的新数据才能支撑后一类主张。有限 episode 的 goodput 不自动成为稳态 SLO 容量。

### 7.5 预算、锁与停止规则

- P0 先生成有限 run plan：逐 case 列问题、输入/配置/种子、比较方法、依赖、实际源码/二进制 hash、argv、数值 timeout/主机内存限制；汇总正常 launch、修复预留和累计 walltime 上限。不能只写“每 case 两次”而无限创建 case。数值预算须由执行任务与已有运行资源依据落实，未填写完整前不启动 backend；不自动继承历史 full 或阶段预算。本次修文不意味着批准一个未列明的大矩阵。
- P0 的静态审计、单测和 recorded replay 不计 backend launch；任何实际后端、debug、fixture、集成或失败均计数。每个已登记逻辑 case 最多一次正常 attempt 加一次有根因、同研究合同的 replacement，重命名不能重置次数。保留所有 raw、失败现场和修复原因；有效同实现/合同的资产核账复用，不为估计确定性模拟的方差反复重跑。
- P3 的 remote 小 case 未通过前，不进入 P6 二维 scan。发现物理语义未定义时，只阻塞对应分支并记录原因，其余独立分支继续；发现内部程序、账本或事件错误时，先修复并补充红绿证据，再按 replacement 规则重跑。
- 合法驱逐、重算、复制、不切换、SLO 不达及性能变差均是结果。协议中有定义且完成清理的 remote 失败可作为负结果；程序异常、字节不守恒或错误释放不是合法失败。不存在一个尚未定义的 MAPE“通过阈值”：单位错误、重复计费、oracle、缺事件等应修复；实现正确但预测误差大则报告模型限制，可以在既定矩阵内评估其影响，不能靠搜索权重保证通过。正式 remote 实验必须有真实可运行的在线路径。
- 所有运行用下列单锁，flock -n 覆盖 runner 与 children；启动前查已有进程，严格单仿真串行。长任务持续登记进度与预算，工具返回/超时不等于后台已结束，不能因此重复启动。主机 RAM/运行超时与模拟 HBM 驱逐是不同问题，前者不得伪装为算法容量负结果。

  `@experiment/strategy_selection/phase3_evidence/runs/.single_simulation.lock`

- 进入 P6 前冻结公式、在线更新规则、安全常数、输入生成规则和对照合同；运行中允许且要求派生参数和负载估计更新。不得根据 scan 输赢重拟合。若发现真实实现错误，保留原结果，修复后明确受影响配对，不能混用修复前后数据宣称公平比较。
- 例行核账、适配、已界定修复和普通负结果按依赖顺序继续，直到统一交付。只有相关物理语义缺失、实现无效或预算耗尽停止对应运行；独立且已登记的工作仍继续。预算不足时仍完成离线汇总，明确缺口，不将“任务已交接”写成科学目标全部完成。

### 7.6 交付物与结论门

计划阶段交付有限的 run_plan、参数来源与更新规则表、输入/配置清单、预算和状态表；状态区分待实现、已实现未实测、已验证、受阻，不能把计划当结果。结果阶段交付 joint_cost_model.md、action_cost_trace.csv、dynamic_switching.csv、hardware_load_scan.csv、remote_validation.md、conservation_audit.json、ablation_summary.csv、baseline_comparison.csv、strategy_spec.md、FINAL_REPORT.md、claims_and_limits.md、reproduce.md。上述名称是待创建产物，不是已存在证据。模型文档应记录每项参数的单位、来源、更新事件、冷启动和时域；CSV 记录所选动作真值与未选动作 NA，正式报告索引原始事件、失败 attempt、版本、预算及未完成项。

进入 P6 的前置是：无 oracle、全部实例候选、驱逐及四动作语义明确，运行时 copy/remote 路径可执行并有交接、ownership、账本和完成事件的 raw 核验，有限矩阵和预算已经登记。当前代码已提供 adaptive/event-recursion E、copy chunk/restore-group 门控、copy 首块/尾流计价和 remote credit 流，但 E 粗驱逐代价模型的排序误差测量、copy 源端 drain 外部性审计及远读闭式估价校准仍是待验证边界，不能把“路径可执行”写成“统一代价已完整正确”。模型准确度和 joint 收益是研究结果，不是必须调到通过的资格线。最终交付说明可支持的自适应机制、模型失效区域、外部对照限制和未覆盖配置；不自动宣称 joint 全局最优或独立泛化。已有报告仅在对应实现、输入和测量有效范围内引用。

### 7.7 证据索引与使用边界

#### 7.7.1 使用规则

- raw 事件、journal、CSV、manifest、source snapshot、hash、run plan、audit、失败现场和正式报告原样保留。本文只保留仍有价值的证据索引，不包含过时策略定义或执行流水。
- 读取证据是为了核验语义、实现和测量，不能根据历史输赢倒推公式、权重或扫描点。当前实验须有自己的输入、源码、配置与事件证据，不能沿用历史 PASS。
- 槽位覆盖、跨实例结算泄漏、重复释放、准备/消费先后关系是持续回归项目。物理资源保护不能变成实例容量掩码。
- 72、571、652 和 full 是开发/回归资产，不是独立留出数据；TP4/TP6 同时改变多个系统变量。未闭合 cohort、未认证 hop、参考缺失或未激活分层路径须单列，不能外推为完整性能与泛化证据。

#### 7.7.2 正式证据路由

按当前任务读取以下正式文件；路径来自已有材料，本次修文未重新核验其中的源码或原始日志：

- **P0（状态机、账本和无 oracle 审计）：** 读取 `@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H0/evidence_reconciliation.md`、`@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H0/source_runtime_map.json`、`@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H1/patch_root_causes.md`、`@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H1/information_flow_audit.json`、`@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H1/test_report.md` 和 `@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J1/issue_capacity_audit.md`。它们提供缺陷根因、事件顺序与审计方法；采用本文的动态驱逐与全候选规则。
- **P1/P3（动作和 remote-read）：** 读取 `@experiment/strategy_selection/phase4_final_strategy/implementation/v1/` 内的动作/账本实现、`@experiment/strategy_selection/phase4_final_strategy/decision_gate_20260910/G3/REPORT.md`、`@experiment/strategy_selection/phase4_final_strategy/decision_gate_20260910/G3/audit/validation.json` 和相关 raw runs。只复用事件字段、ownership/释放检查与物理测量方法；当前模型、在线路由和 cost trace 必须重新实现并实测。
- **P4（硬件派生）：** 读取 `@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J0/topology_contract.json`、`@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J0/input_contracts.json`、`@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J3/tp4_derivation/tp4_export_chain.json`。所有新扫描点仍以 `template/` 的实际拓扑和配置为准；这些文件仅提供已验证的导出链示例。
- **P5（事件闭合回归）：** 读取 `@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H1/SUPPORT_CONTRACT.md`、`@experiment/strategy_selection/phase4_final_strategy/post_G_review_20260910/H2/audit/case3_offline_terminal_reconstruction.json` 和 `@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J2/audit/validation.json`。要求新实现重新产生 raw 级闭合证据，不得用历史 PASS 替代。
- **P6（输入、历史对照和边界复核）：** 读取 `@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J2/REPORT.md`、`@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J3/REPORT.md`、`@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J5/claim_boundary_audit.md`、`@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J6/REPORT.md`、`@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J7/FINAL_REPORT.md` 和 `@experiment/strategy_selection/phase4_final_strategy/continuation_to_delivery_20260910/J7/evidence_index.md`。这些文件只用于输入复用、基线保真检查、历史限制和结果交叉核对，不能用于权重搜索或替代新的 P6 证据。

### 7.8 已有实验记录及其适用范围

以下数字仅概述已有材料所载实验版本；它们是证据索引，不表示本文语义已经完成实现或验证。本次文档修改未重新读取仓库原始日志。后续引用须核对对应代码、配置、原始事件和 `phase5_joint/FINAL_REPORT.md`、`claims_and_limits.md`。

| 记录 | 已有材料中的观测 | 可支持的范围与限制 |
| --- | --- | --- |
| phase5 v2 动作验证 | 四夹具 24/24 动作对排序正确，idle 服务误差 3–31 ms；52 run 守恒审计通过，9,624 次 settle 无失配 | 支持对应实现和夹具的局部结果，不保证当前所有通路正确或跨状态模型准确 |
| phase5 v2 652 在线比较 | joint 的 remote 自主选中约 76–80%；mean E2E 劣于 remote-off 36–50%、劣于 affinity 27–40% | 若执行/测量有效，是该版本策略的退化；不能推导 remote 一般无价值 |
| phase5 v3 聚合链路观测 | 三档 mean E2E 由 10.38/9.63/10.01 s 变为 7.09/7.04/6.29 s，改善 26.9–37.2%；remote-off 仍领先 22.5–36.9% | 反映指定配对中的变化；剩余差距不能仅凭该结果归因于某一资源，更不能证明遥测与配额各自必需 |
| 观测与资源诊断 | 端点除数与聚合链路视角存在差异；带宽翻倍曾伴随 remote 增加及 E2E 增加约 37% | 需要逐链路/端点观测或受控实验定位根因；不是源 HBM 瓶颈的独立证明 |
| cap64gib×652 完成覆盖 | 受限 recompute-only/affinity 臂的 259k-token 会话出现约 266 GB 双持有需求，1/652 未结算；含 remote 臂有完成记录 | 核查唯一 KV、重复量、预留、TP 容量、流式 copy 及时释放、其他实例/原地执行/等待及 remote 完整结算后，才能判断不可替代的完成率收益 |
| 服务指标 | 既有 ε=0.01 的 SLO 目标未达；部分 TTFT/TPOT、独立数据泛化与稳态容量尚无有效覆盖 | 保留负结果和 NA，不以正式交付状态代替科学结论成立 |

定价诊断与在线收益分开：聚合字节/单链路率、decode 上下文缺失、HBM 端点计费、同 tick 流可见性、集合通信覆盖和终态上下文读放大均应对实际代码逐项核查。某一错误的倍率或方向不能替代完整模型验证。执行与计量有效的负结果仍保留；存在相应缺陷的记录标明受影响范围，不作为未执行动作或当前语义的真值。

压力点继续纳入预登记矩阵，全部预定请求进入完成率分母。完成集/共同完成集的条件延迟另报。远读是否提供独有完成能力是待核验问题，不由某个受限策略臂失败直接得出。

相关证据位于 `experiment/strategy_selection/phase5_joint/`：

- `implementation/joint_v1/`、`strategy_spec.md`、`run_plan.md`：对应实现和冻结规格；路径名是证据标识，不是当前方案名称。
- `FINAL_REPORT.md`、`claims_and_limits.md`、`analysis/`：结果、限制与分析。
- `action_cost_trace.csv`、`dynamic_switching.csv`、`hardware_load_scan.csv`、`remote_validation.md`、`conservation_audit.json`、`ablation_summary.csv`、`baseline_comparison.csv`、`reproduce.md`：测量与复现入口，按实际文件位置核实。
- `capacity_validation.json`、`p4_sensitivity.json`、`analysis/v3_contention_summary.json`、`v3_retroactive_*.csv`、`v3_e2e_comparison.csv`、`v3_growth_curve.csv`：容量、配置与链路诊断；离线反事实字段只作预测诊断。

完整 T/J/E 消融、本文 copy 生命周期、匹配治理下 remote 增量以及外部基线能力保真，均须依据对应当前证据单独确认。

### 7.9 机制与验证议程

具体物理路径和状态机见配套《joint机制改造方案_局部统一内存域》。以下为两份文档共同遵循的实验要求。

**1. 动作与概念。** 保留 remote-read 为正式候选；copy 运行时按消费顺序流式迁移与留存，图侧以目标块/恢复组完成和 ack anchor 表达安全交接，执行消费按数据就绪推进。两者共用块管理、传输和资源模型；copy 不另设局部统一内存域，不能依据远读域裁剪 copy 候选。当前源端 KV 账本在 prefill drain（merge 兜底）批量结算已完成交接块，JCM 已将前缀按 C13 8 层块拆分为首块准备和尾流重叠，但尚未把 drain 结算对后续请求的外部性纳入候选 cost；发射后冻结动作与执行位置，块级交接属于 copy 既定动作内部事件。

**2. 容量与结算。** 记录历史在两侧的动态占用、交接重复量、增量与暂存，区分物理占用和未物化预留。copy 最终并集在执行端，轮末元数据结算不能重复释放源块。remote 按实际保留量和预先固定的结算规则处理，不能预设增量较小或总回 home；部分/多源历史按有效块集合核账。发射时只作因果预测，轮末结算使用已生成的真实量，不向发射阶段泄露最终输出。`service_done` 与 `merge_done` 分开，全部成功服务必须具有合法结算路径。

**3. 资源模型。** 当前运行时/JCM 已建模源 HBM、逐跳路径、目标写入/消费读取及通道共享，并按 TP shard/rank 并行与同步计时；后续核对其与 raw 事件的一致性。decode 使用已知上下文；远读量区分远端基础历史和本地增量。供给速率满足消费需求只是阶段性条件，还需检查启动、缓冲和同步；简化 `d ≤ ρ` 不是整轮平价保证，`ρ < 1` 不是系统无收益证明。只对真实相同的首次读流作共同成本处理。

**4. 观测与流量治理。** 同 tick 已提交读流对后续决策可见，集合通信和端点竞争通过有明确覆盖的账本/遥测反映，避免重复计数。配额是待验证的动作级准入机制，不是 instance 掩码。准入等待和背景请求都计入服务指标；没有持续资源承诺就不宣称封顶整轮损失。先验证可解释的预算，再按明确需要登记反馈和迟滞，不默认增加整套控制机制。

**5. 域与边界测量。** 分开记录请求/阶段/状态条件下的预测供给集合 `D_feed`、相对全部适用替代动作（含 copy）的预测经济集合 `D_econ`、在线实际选择及独立同状态实测集合。定义本地参考、δ 容忍度及聚合窗口，不能用单次实际选择代表完整域。观测大小、距离分位数、方向性、瓶颈与预测偏移；不强求简化阈值和实际翻转逐点重合。FACE 与动态条件的严格关系须另行对齐假设后核验，不能直接宣称零负载退化。

**6. 证据等级与结论。** 离线重定价给已有状态上的预测动作与预测差额；未执行动作真值来自独立同状态执行；系统收益来自治理能力匹配的完整在线回放。定价缺陷不抹去执行/测量有效的既有策略退化，也不自动证明 remote 有益。完成率是收益；已有容量失败先核查全生命周期和合法替代方案，再判断 remote 的不可替代性。保留动作不等于证明贡献，负结果不触发为保住叙事而挑点或调参。

**7. 实施依赖与公平对照。** 当前运行时路径已接通，后续顺序为：固定并核验交接/账本语义 → JCM 远读闭式校准与解析单测/离线诊断 → E 粗驱逐代价模型的排序误差测量（条件触发的懒式精化）与 copy 源端外部性审计 → 同状态受控比较 → 匹配治理的在线比较（治理从静态配额三臂开始，不默认启用自适应控制律）→ 负载升降/边界核查 → 有限 DSE 和系统对照。该顺序嵌入 P0—P6，不新增重复实验链。

| 在线比较 | 回答的问题 |
| --- | --- |
| remote-on 无配额 vs remote-off 无配额 | 共同调度下 remote 的增量 |
| remote-on 有配额 vs remote-on 无配额 | 配额机制的增量 |
| remote-on 有配额 vs remote-off 有同等通用治理 | 治理条件匹配后的 remote 增量 |

三臂可作为初步诊断，但通用治理的最终归因须补齐 remote-off 同治理对照。已有 652 和容量压力点保留；不把追平历史百分比差距、出现特定选中率或某种边界形状作为正确性通过条件。正确性错误须修复，准确度和性能如实报告。

**8. 研究范围。** 重算按真实计算、传输、队列和容量状态比较，不将特定锚点倍率推广为全域支配。研究范围包括片上分布式 HBM 和已建模片外池，报告池容量包络与溢出处理；不纳入 SSD 或多晶圆不等于证明其无用。硬件配置可移动供给与容量条件，系统收益不保证单调。论文贡献由实证的新颖性、适用范围和强对照决定，不预设每个结果分支都形成充分贡献。
