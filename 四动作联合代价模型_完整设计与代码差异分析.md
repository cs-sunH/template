# 四动作联合代价模型：顶层建模规范与源码差异分析

> **定位**：面向晶圆级、多轮 Agent 推理的四动作在线代价模型；请求发射与 P→D 边界分别进行一次联合决策，允许 P/D 使用不同实例。  
> **文档性质**：第一部分是本次独立提出的目标设计，不把现有实现当作设计约束；第二部分再与固定版本源码对照。  
> **范围**：只设计这个代价模型及其必要接口，不扩展论文研究范围，不引入长期价值函数、通用优化器或候选内完整事件仿真。  
> **版本**：设计稿 v2.0，2026-09-26。本文是一套完整现行设计，正文、公式与修改清单采用同一语义；源码对照固定在第二部分所列 commit。  
> **实现状态**：目标设计尚未写入仓库。文中的算例是公式检验，不是性能实验；源码对比是静态审查，不是运行验证。  
> **公式阅读**：独立公式使用 `$$`，行内公式使用 `$`；公式编号放在数学块外。GitHub 支持这种格式，其他 Markdown 阅读器需要开启数学渲染。随附离线 HTML 已把公式预渲染，无需联网或安装插件。  

---

## 阅读导航

[第一部分：完整建模](#part1) · [两个决策时刻](#decision-boundaries) · [四动作语义](#actions) · [层级递推与远读流水](#readiness) · [容量与驱逐](#capacity) · [复杂度及仿真限制](#complexity) · [第二部分：20 项源码差异与修改](#part2) · [公式核验](#formula-checks) · [源码证据](#sources)

<a id="part1"></a>

# 第一部分：目标模型怎样计算


## 1. 要解决什么问题，明确不解决什么问题

模型需要决定：prefill 在哪里执行、以什么方式取得历史 KV；prefill 完成后，decode 是继续留在原处，还是换到另一个实例或改用另一种 KV 获取方式。两个决策都比较从当时起到本轮计算及必要结算完成的预计时间。

本版采用**两个阶段决策，而不是 P/D 位置组合搜索**。初始决策考虑默认继续方案下的整轮费用，但只提交 P 阶段及其必要准备。P 完成且 D 尚未提交时，读取真实已发生的状态，再比较剩余费用。进入 D 的事务提交后，本轮位置和访问方案固定，不进行 decode 中的反复迁移。

同一配置内实例同构、TP 布局固定；P 和 D 可以在同一实例，也可以不同。不建立专用 P/D 实例池，不为每个 P 候选枚举所有 D 候选，不额外搜索任意 KV 块布局。**已提交的物理工作和安全义务不可撤销，尚未提交的下一阶段可以重新选点。**

模型必须考虑计算、数据到达、空间、共享资源和结算，但不是第二个仿真器。每个候选只做摘要查询、算术与有限层级遍历，不生成逐 token 预测事件，不复制后端状态。

本版基于普通稠密、自回归 Transformer、MHA/GQA KV，同一会话轮次遵守前驱依赖。初始片上历史来自一个逻辑驻留实例，其他部分可在片外池；P 可在第二个实例生成增量，D 可在第三个实例执行。因只允许一次阶段切换，**一个请求涉及的片上位置最多为三个**。任意多源初始历史、MoE、MLA、滑动窗口、投机解码或跨会话共享不自动属于该范围，扩展时必须重新给出工作量和来源数量界。

**本版不优化未来所有轮次，也不保证全局最优的 P/D 组合。**未来状态影响通过闭环执行观察，不在本轮成本中虚构未来复用收益。一次边界重映射有可能改善选择，也可能没有收益；两阶段策略必须与一次映射对照，不能由设计本身宣布性能提高。

<a id="decision-boundaries"></a>

## 2. 统一终点、两个决策时刻和选择规则

### 2.1 共同工作终点

`service_done` 是本轮响应计算结束；`merge_done` 是本轮必要数据合并、交接确认、请求相关引用和元数据结算完成。模型同时输出二者，选择时统一使用后者。这个共同终点不要求不同候选最后位于同一实例，也不声称它们的后续轮次价值相同。

任何阶段 z 的候选成本都是从本阶段决策时刻算起的剩余完成时间：


**式（1）**

$$
\widehat C_z(x)=\widehat F^{\mathrm{merge}}_z(x)-t_z,
\qquad z\in\{P,B\}.
$$


### 2.2 初始决策：估整轮，但只提交当前阶段

在请求发射时刻 $t_P$，枚举 $x_P=(i_P,a_P)$。每个候选先预测 P 的工作，再预测**继续在该实例、按照 P 形成的布局运行 D**，最后计算合并。这个默认继续方案记为 $\pi_0(x_P)$：


**式（1a）**

$$
\widehat C_P(i,a)=
\widehat F^{\mathrm{merge}}\bigl(P(i,a),\pi_0(i,a)\mid S_P\bigr)-t_P,
\qquad
x_P^*=\arg\min_{i,a}\widehat C_P(i,a).
$$


默认继续不是把 P 动作重复执行一次：P 的 copy 完成后，D 本地读取；P 的 recompute 完成后，D 不再重算；P 的 remote-read 留下哪些源端历史，默认 D 就继续远读那些区间。

选择后提交 P 必需的准备、计算及预留。对默认 D 增长的容量预留只是资源承诺，不是已发出的 D 算子或迁移。它可以在后续阶段事务中按第 14.5 节转移或缩减，不能被无声删除。不得在尚未选出 D 新目标时，把发往该目标的传输算成已与 P 重叠。

### 2.3 P→D 边界：只估剩余工作

边界时刻 $t_B$ 是最后一个 P 计算完成、首输出 token 或其终止状态已知，而本请求第一笔 D 工作尚未提交的时刻。该请求进入 `D_DECISION_PENDING`，其他请求可继续运行。若已知无需任何额外 D 前向，例如首 token 已满足停止条件，直接进入结算，不运行无意义的 D 选点。

读取 $S_B$：真实已有 KV 的位置和就绪状态、尚未完成的 P 传输/确认、当前负载、容量、已发出 token 和采样状态。第二次比较为：


**式（1b）**

$$
\widehat C_B(j,a)=
\widehat F^{\mathrm{merge}}\bigl(D(j,a)\mid S_B,\widehat n_B\bigr)-t_B,
\qquad
x_D^*=\arg\min_{j,a}\widehat C_B(j,a).
$$


已经完成的 P 时间、P 计算量和已完成传输不再计费。尚未完成的已提交义务依然占资源；其剩余工作如阻塞 D 或最终结算，就按依赖计入。第二次预测不读取后端未来完成事件，只使用当前可见剩余量推导 ETA。

### 2.4 原地继续是基准候选，不是“免费本地命中”

原地继续记为 $x_0$：保留 $i_P$、现有数据布局和默认后续访问方式，不新发不必要的物化或迁移。它必须始终进入第二次比较。

如果 P 后历史仍分在源端和 $i_P$，原地继续仍可能是 remote-read；如果完整历史已经在 $i_P$，它是本地继续。源端数据不能只因决策时刻改变就变为本地。原地继续也不重新放到新请求队尾：其等待使用真实调度规则下的接续机会，见第 8.2 节。

实现将 $x_0$ 归并到四动作中的对应 `(i_P, action)` 记录，附 `is_continuation=true`，不作为额外第五种动作。P 曾使用 copy 或 recompute，不代表 D 的继续记录仍要重复 copy 或 recompute。尚未完成的 P 交接由公共 `CommittedObligations` 描述，不通过新增动作重做。

两阶段都枚举全部实例的适用动作，不按容量、距离或边缘位置预过滤。D 阶段也允许“不换实例但改动作”，例如把当前远读的历史复制到 $i_P$ 后本地 decode。

### 2.5 控制代价与平局

预测成本包含本阶段真实控制流程的时间。第二次评估共同花费的控制时间对所有候选相同；改变位置或访问方案时新增的协议、元数据传输和预约工作只进入改变方案。已作为控制门或资源操作计入的部分不得再附加一次任意惩罚分。

只有改变方案的**完整预测剩余成本严格小于** $\widehat C_B(x_0)$ 时才选择改变；成本相同优先继续。初始平局仍按 `(cost, instance_id, action_order)`；D 边界按 `(cost, 非继续标记, instance_id, action_order)`。动作序固定为 `stay, recompute, copy, remote-read`，不按实验输赢修改，不新增 workload 调优的迁移奖励或惩罚权重。


**式（1c）**

$$
\text{switch only if}\quad
\min_{x\ne x_0}\widehat C_B(x)<\widehat C_B(x_0).
$$


共同控制延迟如何进入模型必须由部署假设给出；宿主 Python 执行时间不自动等于目标系统控制延迟。两阶段方案在真实测量中仍需支付第二次决策本身的开销。

输出至少包含：决策阶段、状态版本、P/D 实例与动作、继续标记、预测响应/结算时刻、下一 token 就绪时刻、最终布局、各类物理字节、空间需求、未完义务、主要阻塞和预测长度来源。**分解字段不要求机械相加回总成本**，因为部分工作重叠。

## 3. 时间、字节与符号

所有事件时刻用相对本次阶段决策的纳秒数，最后再加 $t_P$ 或 $t_B$。跨阶段引用保留原事件身份与绝对时刻，换算时只取尚未完成的部分；不能把 P 阶段时间再次平移成 D 阶段的新增等待。所有带宽用 B/ns，工作量用 FLOP，容量用 B。`1 GB/s = 1 B/ns`，GB 与 GiB 不混用；bit/s 必须先除以 8。内部允许浮点计算服务时间，但最终事件时刻向上取整，不用向下截断让工作提前结束。

| 符号 | 含义 |
|---|---|
| $N,p,L$ | 实例数、每实例 TP rank 数、模型层数 |
| $H$ | 已知历史中，具有可复用 KV 身份的 token 数；不代表每层都在片上 |
| $U$ | 本次需要执行 prefill 的新增 token 数，含上一轮尚未进入 KV 的尾 token |
| $\widehat n_D$ | 预测的 decode **前向步数**，不是未经转换的输出 token 数 |
| $q_c$ | 实际执行的 prefill chunk 大小 |
| $g,r$ | 层/层组编号、相对 TP rank 编号 |
| $b_{g,r}$ | rank r 上第 g 层每 token 的实际 KV 字节数 |
| $t_P,t_B$ | 初始发射与 P→D 边界的决策时刻 |
| $i_P,i_D$ | 本轮 P 与 D 的执行实例，可不同 |
| $H_B$ | P 边界实际已有 KV 的 token 数，通常为 $H+U$ |
| $\widehat n_B$ | 边界时共同采用的剩余 D 前向步数估计 |
| $\omega_B$ | 边界快照中的逻辑 home，只用于确定性平局，不等于全部数据所在地 |
| $Q_i^P,Q_i^{D,cont},Q_i^{D,admit}$ | P 接纳、D 原地接续、D 新接纳的计算等待估计 |
| $A_{g,r}$ | 该段必需空间的预测就绪时间 |
| $R_{g,r}$ | 该段必需历史数据的预测就绪时间 |
| $B_{s,g,r}^{B}$ | 边界时来源 s 对层 g、rank r 持有的唯一有效 KV 区间字节 |
| $\eta_e B_e$ | 资源 e 的无排队有效服务能力 |
| $m_e,k_e$ | 已知后台竞争流数、候选在该阶段占用该资源的逻辑流数 |

默认按一层建立一条计算记录，因而 `G=L`。连续层只有在没有不同数据就绪点、不同通信边界和不同资源性质时才允许合并。不以压缩记录数为理由跨越真实等待边界。实际传输块可以比一层大，但必须提供其包含各层的就绪时刻；若执行端整块才可见，模型也必须使用整块完成门。

## 4. 输入从哪里来：只读且因果

### 4.1 静态输入

配置必须给出：模型算子尺寸、数据类型、每层 Q/KV 头数、TP 分片/复制规则、各 rank 权重大小、HBM 容量/带宽、Mesh 路由及链路方向、端口绑定、传输启动/逐跳/控制消息时延、TP collective 算法、prefill/query/KV 分块大小、工作区大小以及有限暂存大小。

这些是模型定义、硬件或执行协议参数，不是根据待测 workload 搜出的系数。缺一项就登记缺项，不把未提供的缓冲、带宽或并行度当作无限。

### 4.2 动态输入

只读取已经到达的请求 token、历史 KV 区间及位置、有效/在用/在途引用、当前实际占用和未物化预留、当前服务负载、在册通信以及已经完成的服务观测。未来 CSV 行、当前请求真实最终输出、未来返回时间、后端预先排好的真实完成事件，不是合法输入。

可用“当前剩余字节和当前速率”预测一个未完成操作的 ETA，但该 ETA 必须标明是预测，不是偷读后端的将来事件时间。


### 4.3 快照、已提交义务和一致性

两个阶段分别取得版本化只读快照，所有候选使用同一阶段版本、预测长度与系数。候选评估不能改变 KV、预约真实空间或登记真实流。提交前若相关状态版本变化，在尚未产生不可撤销工作时废弃该预测、按新快照重评估。重试依赖真实状态事件，不能无变化循环。

P 完成不等于所有 P 事务资源都已经回收。边界快照逐项保存已提交但未完成操作的 ID、源/目标、区间、剩余字节、引用和释放条件。任何 D 候选都继承这些义务；不会用第二个 owner ID 重复发出同一笔未完成传输。

对已经有目标写完成、但源端 ack 尚未结束的复制，目标数据可按真实写完成门被消费，源副本仍占空间。D 需要继续读某个源副本时，要先原子地取得该副本的新阶段引用，再结束 P 引用；若旧交接已不可撤销地安排源释放，则使用其已确定接收端或等待接收端就绪，不复活已释放副本。

第二次“选择位置”可以在旧确认尾部仍在途时进行，不必等待无关尾部全部排空；实际读写只能在各自依赖满足后执行。最终 `merge_done` 还需等本请求所有必要交接义务闭合。**剩余义务可能影响不同 D 候选不同的关键路径，不能当作相同常数全部忽略。**

## 5. 先把 token 与 KV 的身份对齐

### 5.1 可复用的前提

只有模型版本、token 序列前缀、位置编码、精度和 attention 语义兼容时，历史 KV 才能复用。其他 Agent 传来的“信息”不自动等于当前请求可直接复用的 KV。当前设计针对已经通过语义校验的同一逻辑历史。

### 5.2 输出 token 不一定已经有 KV

本版采用实际自回归执行口径：prefill 可以生成第一个输出 token 的 logits；之后每次 decode 前向把上一个输出 token 写成 KV，并生成下一个输出。于是，正常生成 `D≥1` 个输出时，有 `D−1` 次额外 decode 前向，最后一个输出 token 可以尚未进入 KV。

会话元数据分别保存“已有 KV 的 token 数”和“待在下一轮消费的尾 token”。下一轮把该尾 token 拼进实际 $U$。如果执行协议选择额外前向把最后 token 也物化，则必须把该前向、KV 和时间显式计入；两种约定不可混用。

估计器直接学习已经完成请求的 **decode 前向步数**。因此容量和通信式使用前向步数，不再重复做 `−1`。初始请求规范化后要求 $U\ge1$；真正的空请求可以是零工作元数据事务。

P→D 边界通常有 $H_B=H+U$ 个 token 的 KV，以及一个已经产生但尚未写入 KV 的首输出 token。第二次映射只转移必要的已物化状态和控制状态；第一步 D 再消费这个首 token。不得因为转移了该 token 的 token ID，就将它也算成已经具有各层 KV。

D 阶段的每次前向输入为一个实际已产生 token。copy/remote 的 D 准备不重新执行原来的 U-token prefill；recompute 若被选中，只重建 $H_B$ 的已知上下文，随后才运行正常 D。重建过程丢弃重复 logits，不重复输出首 token、不再次采样、不推进已保存的随机数状态。

元数据交接包括 token 身份、位置索引、请求/模型配置、停止状态及采样/RNG 状态；用真实编码长度计控制字节。选择 recompute 时，重放所需 token ID 序列若不在目标或其可访问控制存储中，还须支付该已知序列的真实读取/传输，不假定它免费存在。预测器使用当前请求和已知 token 的描述，不能通过这类字段引入未来长度。

### 5.3 逐 rank 字节公式

对第 g 层，设 rank r 实际持有 $h^{KV}_{g,r}$ 个 KV 头，每头维度 $d_g$，每元素 $s_{KV}$ B，则：


**式（2）**

$$
b_{g,r}=2h^{KV}_{g,r}d_gs_{KV},\qquad K_{g,r}(n)=n b_{g,r}.
$$


这里必须用 **KV 头数**，不是 Q 头数。不能将总字节机械除以 TP：头数不整除、KV 头复制及 padding 都要按物理布局记录。逻辑有效载荷、物理 padding、真实副本三项分开。


对任一阶段 z 的基础上下文，令 $H_z$ 为具有确定 KV 身份的 token 数：初始历史取 $H_P=H$，P→D 边界取 $H_B$。其唯一有效内容满足：


**式（3）**

$$
\sum_{s\in\mathcal O_z}B^z_{s,g,r}
+B^z_{\mathrm{pool},g,r}
+B^z_{\mathrm{absent},g,r}
=H_z b_{g,r}.
$$


$\mathcal O_z$ 是本阶段的片上持有位置集合。初始最多一个片上历史来源；P 边界通常最多两个。D 新目标若不在这两个位置中，会形成最多三个片上位置。池不是片上实例，不被当作 remote-read 的直接片上来源。

等式只数唯一有效内容，不重复计算交接中的第二份副本。账本必须同时保存“可读物理副本”和“唯一逻辑区间归属”；真实容量要计所有仍未释放副本。

位置记录至少包含 `(层区间, token区间, TP rank, model_id, owner, replica_id, ready_event, release_event)`。同一层内，旧历史可以在 home，本轮输入在 $i_P$；不能只保存每层一个 home 字节数就推断所有 token 的位置。预测器只遍历区间摘要，不逐 token 建对象。

基础内容若确实已删除、无可用或已提交恢复路径，则计入 `absent`。stay/copy/remote 不能免费复用它；recompute 在具有已保存 token 时仍可重建。某动作不适用不删除整个实例。

对目标 j，选读副本的规则固定为：目标已有有效副本优先；否则使用该区间当前合法 owner；owner 已有不可撤销交接时使用其确定接收端，并保留就绪依赖。相同等级副本按稳定副本 ID 决定，不搜索任意副本与任意目标的组合。已在向 j 传输的内容不重复发送。

若源记录出现无法合并的区间数随输入长度无界增长，应修复区间合并或报告超出本版布局范围，不悄悄把其复杂度算成常数。GQA 的 Q/KV 头区别属于基础模型性质，见 [A1](#a1)，不是本方案的新增机制。

<a id="actions"></a>


## 6. 四动作的语义：按当前阶段状态解释

四动作在两个决策点使用相同的数据来源和资源规则，但处理的工作不同：初始阶段还需要执行新增输入的 P；D 边界只处理现有上下文的接续、必要准备及剩余 D。**动作名不授权重复执行已经完成的工作。**

### 6.1 stay：利用目标已有历史

初始阶段，目标具有可用本地历史时直接复用；片外缺失层可恢复到目标。没有历史的新请求在任何目标均可本地建立。

D 边界，目标已有本阶段基础上下文所需片上部分，其他部分仅需已承诺准备或池恢复时，可选 stay；若还需要从另一个片上位置新搬一段输入 KV，应归入 copy，而非零传输 stay。

原地继续按实际状态归一化：P 通过 copy/recompute 已形成完整本地上下文，D 继续为 stay；P 后仍在远读，D 继续为 remote-read。已有准备和确认尾部由义务表继承，不重复发出。

### 6.2 recompute：重放本阶段确定的已知上下文

初始候选从已保存 token 重放 $H+U$，历史计算基数为 0，不依赖旧 KV。D 边界候选从已保存 token 重放 $H_B$，历史基数也为 0；重建后执行 $\widehat n_B$ 次正常 D 前向。

第二次重算是新选择所付出的真实额外计算，不是把旧 P 时间重新加进成本。重建阶段不重复向用户输出、不重新采样，遵守第 5.2 节的 token/RNG 规则。

本版不采用“缺失一半层就重算一半历史 token”的折算。KV 不是任意层边界的完整激活检查点；没有检查点依赖证明，不能直接假定只算后缀层，见 [A2](#a2)。

目标上本会话无其他引用、未被已提交操作使用的旧分配，可以在提交后转作重建空间；这是空间复用，不是免除算子工作。异地旧副本与承诺释放只按真实安全事件处理，不提前抵扣目标空间。

### 6.3 copy：只补目标缺少的区间并持久留存

从当前合法片上来源迁入目标尚缺的有效区间，池中部分按池路径恢复。目标已经拥有、或已有不可撤销传输在补齐的内容不重复搬运。P 初始通常只有一个片上来源；D 边界可能有两个。

某层的所有必需区间写到目标后，该层 attention 才消费；仅选择读取基础区间不会生成新的 token KV。不同层可与计算交叠，但本版不假设同一层的完整 copy 与 attention 细粒度交叠。多来源同层准备按各自通道完成，取该层所需区间的最晚就绪时刻。

源块仅在目标写完成、确认到达且所有旧/新阶段引用结束后释放。D 回到历史 home 时，只补缺少的 P 增量；不能无条件按全部 $H_B$ 再收一次迁移费。copy 完成后，D 在目标本地读取，并集留在该目标。

### 6.4 remote-read：只远读当前目标缺少的片上区间

目标本地基础区间直接复用；仍由其他片上位置持有的基础区间按消费需要远读；池中缺失部分只恢复一次到目标。P 初始的输入在 $i_P$ 生成；D 新增 KV 在 $i_D$ 生成。原先在 $i_P$ 的输入，对另一个 D 目标可能成为远读基础的一部分，不能继续把所有“本轮输入”概念性地当作目标本地。

每次读取进入有限 HBM 双缓冲，attention 消费后才可复用缓冲。多来源按确定 owner 顺序和 token 区间顺序消费，共用同一对缓冲；本版在来源边界排空后再开始下一来源，不同时为每个来源另建一对缓冲。来源数量受第 1 节约束，不搜索各区间留存/远读的任意组合。

仍采用第 12 节的层内双缓冲协议，不跨层预取。源区间从建立 D 引用到最后一次 D 读取结束都受保护。轮末合并按最终实际持有位置执行第 15 节。

### 6.5 一个必须能正确表达的 D 边界状态

历史 H 在 h，P 在 $i_P\ne h$ 用 remote-read 完成，新增 U 的 KV 在 $i_P$。D 继续在 $i_P$ 时，远读的是 H；D 回 h 并 copy 时，只需补 U；D 到第三个实例 j 并 copy 时，需要从 h 和 $i_P$ 分别补 H、U。D 到 j 并 remote-read 时，两个来源都可能被反复读取，j 只保存池恢复部分与之后的 D 增量。

这四种状态都必须由实际区间账计算；不能把“回源”固定等同于全量迁移，也不能让原地继续免费得到 H。所有候选得到相同的逻辑计算结果。

## 7. 长度估计：常数空间、没有未来信息

每个会话保存 `(count,sum)`；运行级再保存一组 `(count,sum)`。完成后更新实际 decode 前向步数：


**式（4）**

$$
\widehat n_D(s)=
\begin{cases}
\lfloor sum_s/count_s\rfloor,&count_s>0,\\
\lfloor sum_{run}/count_{run}\rfloor,&count_{run}>0,\\
32,&\text{无样本}.
\end{cases}
$$


`32` 是本版冻结的冷启动先验，单位为前向步，不是硬件可推导真值，更不能称为免假设。首次完成前显式标记冷启动；评估该先验的敏感性，但不按每份 workload 挑最优值。

不保存全部历史样本，不为计算均值扫描过去轮次。每会话统计为常数空间；会话显式结束时可以删除，仍可能返回的会话不能仅因估计器想省内存而静默丢状态。

P 初始使用式 (4)预测整轮额外 D 前向。P→D 边界重新读取同一因果估计器，但不把 P 结束当作请求已经完成的训练样本。边界没有实际执行 D，因此不能把 P 的输入 token 数从预测 D 步数中扣除。

若已观测到停止状态，跳过 D 决策；否则此时至少还要一次前向，边界估计取：


**式（4a）**

$$
\widehat n_B=\max\bigl(1,\widehat n_D(s;S_B)\bigr).
$$


所有 D 候选共用该值，不因目标或动作不同而分别选择更有利的长度。已知客户端硬上限可以限制估计，但不能使用 trace 中尚未实现的真实最终长度。P 期间其他请求已经完成，因而在线均值可能更新；这是合法新信息，不是对未来输出的观测。

预测偏短不能截断请求。P 阶段的预测预留在 D 边界按新方案和新估计重新结算；进入 D 后超过预留则在同一 D 位置/动作下扩容和等待，不触发第三次选点。真实长度与预测差额单独报告。


## 8. 当前负载怎样变成目标等待

### 8.1 P 接纳与共同服务近似

采用一个明确近似：把公共调度规则安排在本候选之前的未完成工作折算成等效服务时间。对阶段 z：


**式（5）**

$$
Q_i^z=\widehat R_i^{z,\mathrm{ahead}}
+\sum_{u\in\mathcal Q_i^{z,\mathrm{ahead}}}\widehat S_u.
$$


运行中剩余量来自已观测进度。请求自身不计入自己的等待；同一工作不同时完整出现在 running 与 queue。仍活跃、但长度均值已耗尽的工作至少保留一个预测前向步，不能当作消失。

这是批处理/混合调度的等效近似，不是精确完成时间或可靠上界。调度器在入队、进度、完成时维护前缀工作量或等价摘要；本次查询不逐候选重扫所有请求。公共调度政策不因哪个动作更有利而改变。

### 8.2 D 原地接续与 D 新接纳分开

P 边界至少区分两个值：


**式（5a）**

$$
Q_{i_P}^{D,\mathrm{cont}}
\quad\text{and}\quad
Q_j^{D,\mathrm{admit}}.
$$


原地继续继承公共调度规则下的下一次合法接续机会，不重新排到新请求队尾，也不一律假设为 0。迁往 j 的候选使用 j 的真实接纳位置及下一个允许加入的 batch/迭代边界。相同位置但改变访问方式仍保留其合法接续顺序，另支付数据/容量/控制准备。

快照提供当前批处理边界、接续票据和新接纳摘要。边界票据只表示次序，不是免费独占计算资源；等待决策或数据期间其他请求仍可推进。被公共规则允许与本候选并行的任务进入资源份额，而不是又完整加到 Q。

### 8.3 等待、资源竞争和未完成 P 工作去重

本次 Q 已认为会在计算前排空的工作标为 `queued_ahead`：提前准备阶段仍可竞争，候选计算阶段不能再完整收费。实际并行的任务标为 `concurrent`，计份额；不可同时按全部剩余服务加到 Q，又完整保留为并行流。

D 边界继承的 P 传输/确认使用其真实 owner 和剩余依赖。已完成 P 算子不在任何 D 等待里重现；尚未完成的传输可能继续占端口。源实例和路径后台工作不会因目标 Q 结束而自动消失。不确定释放时刻时标记 `contention_release_uncertain`。

**Q 只门控计算，不自动门控全部准备。**允许的 D 迁移从 $t_B$ 之后真实控制门开放时才开始，可与 D 排队重叠；不能追溯与已经完成的 P 重叠。总时间按统一就绪递推求得，不直接写成 `Q + 所有准备 + 所有计算`。

## 9. 共享资源怎样计价

### 9.1 资源必须具体到位置与方向

HBM 按物理 rank 分开；NoC 按有向链路分开；池按实际端口分开。共享同一端口的流竞争，不同端口不被错误串联。正向与反向路由分别计算。

当前已知后台流数记为 $m_e$。本候选阶段内可能并行使用该资源的逻辑流数记为 $k_e$。每条候选流在该资源上使用：


**式（6）**

$$
\mu_e=\frac{\eta_e B_e}{m_e+k_e},\qquad k_e>0.
$$


同一端口上的计算读取、copy 写入、pool 恢复写入和 remote 暂存写入，必须出现在同一个分母中；同一计算流先后读权重和 KV，不拆成同时存在的多个流。每条路径上的不同 rank 候选流，要计算自身共享。

本版按阶段冻结份额：P 初始准备、P 计算、P→D 转换、D 本地/远读服务、最终 merge 分别有自己的候选占用集合。第二次选点刷新当前状态；尚未结束的 P 流按原 owner 的剩余义务出现，不能作为“新迁移”再登记一遍。同一阶段即使某条候选流暂时空闲，也不重新跑逐事件仲裁。该“预留份额近似”通常损失一些重叠精度，但开销有界；它不是对真实运行的确定性上界。

**冻结的是速率估计，不是状态跨决策不更新。**下一次决策重新读取当前负载。

### 9.2 不能把总体吞吐倒数当作流数

链路总体吞吐接近峰值，不代表只有一条流。后台竞争数量优先来自流登记/流数遥测。两者反映同一流量时不相加：若可按流身份去重，就取并集；否则本版冻结为 $m_e=\max(m_{\mathrm{registered}},m_{\mathrm{telemetry}})$，两项都只含旧流，再单独加候选的 $k_e$。时间窗遥测不等于瞬时真值，记录窗口范围及过期状态；来源冲突必须标明。没有流数、只有总体吞吐时，不用 `B_peak/aggregate_throughput` 冒充仲裁除数。

无争用效率 $\eta_e$ 的冷启动值为 1。可使用已完成、能分离服务段的事件校正：观测量应先剔除已计价的排队与争用，不能把拥塞同时写进 $\eta$ 和 $m_e$。分不开就不更新该样本。更新采用固定 EWMA 系数 $1/16$：$\eta_{new}=\frac{15}{16}\eta_{old}+\frac{1}{16}\eta_{sample}$。可接受的样本必须有明确的无争用等价服务量；$\eta_{sample}=\frac{\text{独占峰值服务时间}}{\text{去掉排队与已建模竞争后的观测服务时间}}$。若无法可靠去掉竞争，直接拒收，不用总体请求时延拟合。有效样本必须满足 $0<\eta_{sample}\le1$；不满足时记录异常并跳过，而不是无声截断。首次有效样本也按同一更新规则处理，所有配置从初值 1 开始。这是数值估计规则，不按 workload 拟合。

### 9.3 一条传输怎样计算

对流 f，字节量为 D，实际经过资源集合 $\mathcal P_f$：


**式（7）**

$$
X_f(D)=\lambda_f+\max_{e\in P_f}\frac{D_{f,e}}{\mu_e}.
$$


零字节无事务时返回 0。$\lambda_f$ 包括真实需要的源/目标启动、逐跳、池与控制时延。不同资源若处理不同字节量，就使用各自的 $D_{f,e}$。不能重叠的阶段分开调用式 (7)，再通过顺序相加。

对流水 Mesh，一份 D B 数据在 h 跳上分别服务，不把 `hD/B` 当作必然端到端时间；h 进入路径资源占用和逐跳时延。`hD` 可以是链路字节总量，但不是无条件的时延公式。若实际网络采用整消息逐跳存储转发，则必须改用对应的串行传输公式，不能把流水假设偷偷套上去。

copy/pool restore/merge 都包括目标 HBM 写入；remote 还包括随后 attention 的缓冲读取。后者是第二次真实访问，不是重复计费。池路径必须包含池端口、边界到目标的 NoC 和目标 HBM；写回方向包含源 HBM、到边界的 NoC 和池端口。

多流完成时间由每条流的式 (7) 加固定阶段顺序，再取必要的最大值。不再额外叠加同一组“竞争等待”。

## 10. 计算工作量：从算子尺寸推导，不按历史比例乱折算

### 10.1 基础算子服务

每层分为少量固定算子段：投影/新 KV 写入、attention、输出投影和 FFN，以及明确的 TP collective。每个本地算子 o：


**式（8）**

$$
C_{o,r}=\lambda_o+\max\left(\frac{F_{o,r}}{\eta_{P,r}P_r},\frac{M_{o,r}}{\mu_{HBM,r}}\right).
$$


这里的 max 是本地算子的 roofline 近似，不意味着所有不同算子都重叠。算子顺序不同就相加。权重读取、激活读写、KV 写入分别记账；同一操作不在另一个服务段重复计算。

权重、激活和工作区的字节不是一个待拟合系数。每个固定 kernel 模板必须给出读取张量、写入张量、元素数、精度、驻留位置与重复访问次数。若张量 x 位于 HBM，则 $M_o=\sum_x\operatorname{elements}(x)\,\operatorname{bytes}(x)\,\operatorname{access\_count}(o,x)$；片上驻留且未触碰 HBM 的访问不加入这一项。量化权重的解码/scale 访问也属于模板。工作区采用这个固定 kernel 序列中尚未释放临时张量的最大同时存活字节；不创建真实 tensor 数据。

模板字段不全时，该模型配置报 `missing_kernel_model`，不能将缺失算子解释为零开销后继续做正式性能结论。

### 10.2 可直接检查的 FLOP 例子

设一个 chunk 有 q 个 query，进入 chunk 前上下文有 h 个 token，隐藏维度 d，Q 总维度 $d_Q$，KV 总维度 $d_{KV}$，FFN 中间维度 f。以乘加算 2 FLOP：


**式（9）**

$$
F_{QKV}=2qd(d_Q+2d_{KV}),\quad F_O=2qd_Qd,
$$


**式（10）**

$$
F_{attn}=4h_Qd_h\left(qh+\frac{q(q+1)}2\right),
$$


门控三矩阵 FFN 的矩阵乘部分为 $6qdf$；两矩阵 FFN 为 $4qdf$。归一化、激活函数、RoPE、softmax、采样和词表投影不能因此自动变成零：按静态 kernel 模板记工作量，或显式登记未建模的共同小项并测量其量级。

式 (9)-(10) 是由算子尺寸得到的工作量式，不是某个现成 kernel 的精确时延。逐 rank 用实际持有的 Q/KV 头、权重和复制布局求值。不能把 GQA 的 Q 头数替换成 KV 头数计算 attention FLOP。

### 10.3 HBM 与 attention 分块

不物化 $q\times\mathrm{context}$ 的完整 score 矩阵；采用有固定 query tile 和 KV tile 的流式 attention 模板。query tile 大小来自执行配置。每个 query tile 对历史扫描一次，因此一个 q-token chunk 的历史扫描次数为：


**式（11）**

$$
\nu(q)=\left\lceil\frac q{q_{tile}}\right\rceil.
$$


模板若能在 SRAM 内保留更多状态，可以改变扫描次数，但必须明确执行协议，不根据预测器偏好减少读量。FlashAttention 的分块/有界中间状态思想属于已有技术，见 [A3](#a3)；本模型只借用这一可执行前提，不将其作为贡献。

对于 remote，每个 query tile 只远读源端仍保留的基础历史。输入、已生成 token 和已恢复后缀本地读取。各来源 attention 部分通过同一次流式 softmax 更新形成完整结果，归并工作记在 attention 模板中。

TP collective 不用“总字节除以一条链路”估计。静态展开配置指定的 collective 通信轮次，每轮按实际流路径和式 (7) 求值；轮次串行，轮内必要流取最大值。该静态展开可跨候选复用，不创建请求执行事件。

<a id="readiness"></a>

## 11. 层级数据就绪递推

### 11.1 一次性的 copy / pool restore

每个 rank 的可用传输通道数 $C_{\mathrm{chan}}$、每类通道的顺序和最大在途数来自实际执行配置；不能由预测器暗设成一条，也不能暗设成无穷条。本版确定性地按消费层序，把下一传输分配给该类允许通道中预计最早空闲者，平局取通道编号。该选择只安排当前候选内的已知工作，不搜索所有排列。若执行器已经有固定通道映射，则优先复用该映射。

每个 rank 的同一传输通道按层消费顺序串行。对通道 c 中第 g 项：


**式（12）**

$$
R_{g,r}^{c}=\max\left(R_{prev,r}^{c},A_{g,r}^{dest},S_{g,r}^{source}\right)+X^c_{g,r}.
$$


首项前驱完成为 0。`S_source` 是已知有效源块就绪时刻；`A_dest` 是目标空间就绪时刻。源端当前数据尚在交接时，不能假定已经存在。

片上前缀复制和片外后缀恢复可以属于不同通道并行；如果共享资源，式 (6) 已对其份额计价。通道内是顺序关系，通道之间不是免费独占资源。固定阶段 $k_e$ 按该阶段实际允许的候选通道数及端点消费流计算，不能把同一串行通道上排队的所有层组都当作同时发流。已有可并行的恢复组不应为了预测器简化而被强制改串行；映射近似与真实调度差异要测量。不能再将不同通道的总时间无条件相加。

本版为需要完整本地历史的 stay/copy，先取得该阶段所需的预测峰值承诺（扣除已有合法 O/Z），再启动其恢复/复制。这样不会隐含使用尚未获得的目标存储。它允许与排队重叠，但不假设逐层扩容给出免费的内存流水。

### 11.2 本地消费层

令上一层 TP 同步结束为 $F_{g-1}$，投影等前段服务为 `Cpre`，attention 本地消费为 `Cattn`，后段含必要通信为 `Cpost`：


**式（13）**

$$
\begin{aligned}
P_{g,r}&=\max(F_{g-1},A^{work}_{g,r})+C^{pre}_{g,r},\\
F_g&=\max_r\left[\max(P_{g,r},R_{g,r})+C^{attn}_{g,r}+C^{post}_{g,r}\right].
\end{aligned}
$$


首层 $F_0=Q_i^z$，其中 z 是当前接纳/接续阶段。层内 collective 的同步必须按其实际位置插入；式 (13) 是没有中途额外同步时的简写，不能把前段 TP 同步遗漏。实际实现可以在每层固定的几处同步取 max，仍然是数组递推。

本地已经可读的区间就绪为 0；copy 是目标写完成；池恢复使用恢复完成；已有在途交接使用实际依赖的预测剩余时刻。某层有多个必需区间时，R 取这些区间的最晚就绪。P 初始的首层使用 $Q_i^P$；D 首步使用 $Q_i^{D,cont}$ 或 $Q_i^{D,admit}$。recompute 重建首先执行其前面的上下文 chunk，再进行正常前向，不能跳过这段新付出的工作。

### 11.3 为什么不用整体 max

若第一层数据已到、计算 1 ns，第二层数据到达为 100 ns、计算 10 ns，则最终为 `max(1,100)+10=110 ns`。`max(全部数据准备100,全部计算11)=100 ns` 会漏掉数据到达后必需的计算。式 (13)保留了这条依赖，并且每层只做有限次运算。

## 12. remote 的有限缓冲与块流水：不逐块生成事件

### 12.1 缓冲大小与规则

每个 rank 为 remote 预约两个大小为 $b_{\mathrm{tile},r}$ 的 HBM 缓冲，合计 $2b_{\mathrm{tile},r}$。它们包含 K 和 V，不再分别漏算一半。tile 大小来自固定执行配置，并按一个有效 token KV 的粒度向下对齐；对齐后不能容纳一个 token 时，该执行协议不受此配置支持，不能改成无限缓冲。

同一时刻最多一个块正在传入、一个块正在消费。块被消费后，缓冲才可复用。只预取当前层；一层计算结束后缓冲可用于下一层。基本 credit 单位是实际字节块，不是“若干未来 decode 步的全部终态上下文”。

### 12.2 两阶段流水的定义式

一个 query tile 对某个当前远端来源要读取 n 个 KV 块，传输时间 $x_j$，attention 消费时间 $c_j$。传输含源读、NoC、目标暂存写；消费含暂存读和对应 attention 计算。若 attention 的投影前提于 `P` 就绪：


**式（14）**

$$
\begin{aligned}
T_1&=S+x_1, & A_1&=\max(P,T_1), & F_1&=A_1+c_1,\\
T_2&=A_1+x_2, && & F_2&=\max(T_2,F_1)+c_2,\\
T_j&=\max(T_{j-1},F_{j-2})+x_j, && & F_j&=\max(T_j,F_{j-1})+c_j\quad(j\ge3).
\end{aligned}
$$


其中 `S=max(上一层完成、两个缓冲已获得、源数据就绪、允许发读的协议时刻)`；第一块后，第二块要到 $A_1$ 才发出。这是本版明确选择的首块发射约束，略比一般双缓冲更保守，不声称双缓冲硬件天然只能这样工作。它把投影长等待期间的预取固定为一个块，执行器与预测器必须采用相同规则。j=1 或 j=2 时只执行存在的分支。式 (14)仅用于解释依赖和单测，不在生产预测器里生成 n 个记录。

### 12.3 生产路径的闭式折叠

对 n 个等大、相同服务时间的块，第一块开始于 S，令 $A=\max(P,S+x)$，则本版首块规则下：


**式（15）**

$$
F=A+c+(n-1)\max(x,c).
$$


n=0 时不产生远读，n=1 时退化为 $\max(P,S+x)+c$。最后不足一个完整块时，先用式 (15)计算前 $n-1$ 个完整块的最后传输/消费时刻，再显式应用一次式 (14)处理尾块，不能把尾块错误放大成完整块。

对于相同大小的 query tile，其远端基础历史字节相同、完整 KV 块消费工作相同，因此可共享完整块服务；最后不足一个完整 query tile 的查询单独求值。启动和逐跳时延按**每个实际事务块**计入 `x`，不是整轮只付一次。

多个 query tile 串行执行。第一个可与本层投影重叠，用 $A=\max(P,S+x)$；后续 tile 在前一 tile 的远端和本地 attention 均结束后启动，其首块等待从该结束时刻起计，不得为每个 tile 再叠加一次整层投影。相同完整 tile 的远读部分用乘法折叠，本地新增上下文随 tile 位置增长的工作另用算术和式计算。

在本版固定大小稠密 kernel 下，本地 attention 的 FLOP 和 HBM 服务项对 query-tile 索引 j 是仿射函数。因此 $\sum_j\max(a+bj,c+dj)$ 最多在两条直线的交点处分成两段，每段用等差和求值；计算交点后用相邻整数复核边界，平行时直接选较大者。按 rank/算子分别计算，rank 同步的 max 必须保留其实际位置，不能把 `sum(max)` 偷换成 `max(sum)`。若 kernel 有额外非线性切换，模板须提供固定数量的分段边界；没有合法分段模板就报建模缺项，不能退化成按所有 query tile 循环。

远读区间与目标本地区间的 attention 都属于同一层。多个片上来源按第 6.4 节的确定顺序逐来源消费，每个来源内部使用上述闭式并处理尾块；切换来源时复用同一对已排空缓冲。最后处理目标本地区间，使用同一次流式 softmax 的统计归并，不重复归一化成多个独立 attention。只有第一个来源的第一块可以与本层尚未完成的投影重叠，之后各来源不重新添加投影时间。层后计算必须等全部必需区间消费结束。

**这个闭式只对冻结服务时间和上述双缓冲协议精确。**后台流动态变化、kernel 实际 tile 组织不同，都属于预测误差来源；不是对真实后端的无条件精确结论。

## 13. prefill、decode 很长时怎样保持固定计算量


### 13.1 两个决策点分别计算哪些工作

| 决策点/候选 | 新执行的重建或 prefill | 正常 decode | 不可重复收取的部分 |
|---|---|---|---|
| P 初始 stay/copy/remote | U-token prefill，初始历史 H | 以默认继续方案估 $\widehat n_D$ 次 | 没有发往未知 D 目标的“提前迁移” |
| P 初始 recompute | 从 0 重放 $H+U$ | 以默认继续方案估 $\widehat n_D$ 次 | 默认 D 不再次重算 |
| D 边界 stay/copy/remote | 没有新的 U-token prefill；只做必要状态准备 | $\widehat n_B$ 次 | 已完成 P 和已经搬到目标的区间 |
| D 边界 recompute | 从 0 重建 $H_B$，不采样、不输出 | $\widehat n_B$ 次 | 已发生 P 是沉没时间；但本次重建是新工作，必须计费 |

初始 P 或新重建按 $q_c$ 分块，尾块单独计。每一段工作的**第一个真实完整模型前向**用第 11/12 节逐层求值，以捕捉初次恢复、复制或读取等待；剩余同类前向用第 13.2 节折叠。D 边界需要迁移时，第一步 D 而不是原来的 P 再次受准备门控。

第一个需要全模型历史的前向结束后，供其消费的一次性载荷必须已经到位；但 ack、源释放等尾部可能仍在途。后续步骤不重复加载已经留存的载荷，未完尾部仍按其实际引用/端口占用存在。不能把“数据已消费”和“该事务全部资源已释放”等同。

### 13.2 长重复段的固定 8 区段规则

对剩余 m 个等长 prefill chunk，或 m 个 decode 前向步，使用 $K=\min(8,m)$ 个连续、尽量等长区段。第 j 区段含 $w_j$ 次前向，索引为 $[l_j,r_j]$。在左右端点和中点分别评价当前冻结资源模型下的一次前向服务函数 $\tau(z)$：


**式（16）**

$$
\widehat T_{repeat}=\sum_j w_j\,\tau((l_j+r_j)/2).
$$


m≤8 时逐次求值，结果对该服务模型精确。中点可以对应非整数的上下文长度，仅用于解析服务估值；真实字节总量和空间峰值始终按整数公式另外计算。

区段端点明确取 $l_j=\lfloor(j-1)m/K\rfloor+1$、$r_j=\lfloor jm/K\rfloor$、$w_j=r_j-l_j+1$。m=0 时整个重复项为 0，不构造区段。左右端点的两次额外求值用于诊断；关闭该诊断不改变中点选择结果，只减少常数开销。

若固定资源模型中的 $\tau$ 随上下文单调，则同一模型内有诊断区间：


**式（17）**

$$
\sum_jw_j\tau(l_j)\le\sum_{z=1}^m\tau(z)\le\sum_jw_j\tau(r_j).
$$


这个区间只约束当前冻结服务模型的分段近似，不约束真实后端误差，也不是候选的真实性能证书。已知非单调 kernel 阈值需要在模板中显式给出；本版不据式 (17)做候选剪枝。

`K=8` 是固定数值精度预算，不是新机制，不根据哪个值让 joint 跑赢而调整。验证可比较 K=4/8/16 的选择差异；本版生产规则冻结为 8。


### 13.3 远读与 KV 增长：先确定当前远端区间，再计算遍数

令 $B^{z,\mathrm{off}}_{g,r}(j)$ 为阶段 z 中，目标 j 缺少、选择 remote-read 且仍在其他片上来源的基础区间字节和。目标本地区间和池恢复后已留存区间不包含在此量中。

初始 P 的默认 remote 方案：


**式（18）**

$$
D^{P,\mathrm{remote}}_{g,r}
=B^{P,\mathrm{off}}_{g,r}(i_P)
\left(\sum_c\nu(q_c^{\mathrm{actual}})+\widehat n_D\right).
$$


D 边界的剩余远读量：


**式（18a）**

$$
D^{B,\mathrm{remote}}_{g,r}(j)
=B^{B,\mathrm{off}}_{g,r}(j)\,\widehat n_B,
\qquad
B^{B,\mathrm{off}}_{g,r}(j)
=\sum_{s\in\mathcal O_B,\ s\ne j}B^{B,\mathrm{read}}_{s,g,r}(j).
$$


$B^{B,read}$ 使用第 5.3 节确定的实际选读区间并去重。D 边界已在 $i_P$ 的 U-token KV，对另一个 j 是基础上下文的一部分；如果选择 remote，它可能进入式 (18a)。只有 **D 边界之后在 j 新产生**的增量才必然不进入边界基础远读量。P 以前已读过的字节不是 D 本次的新增费用；继承的未完成 P 读流另由义务表核账，不能混成新的 D 扫描。

D 准备的 copy 字节为目标缺少、且尚无可复用目标副本或已提交到目标传输的基础区间；不能一律取 $H_Bb_{g,r}$。池恢复与源端 copy 按各自来源单独计。

给定基础上下文 $H_z$ 与后续 n 次正常单 token 前向，累计逻辑 attention 上下文为：


**式（18b）**

$$
\sum_{k=1}^{n}(H_z+k)=nH_z+\frac{n(n+1)}2.
$$


P 默认 D 的 $H_z=H+U$；边界 D 的 $H_z=H_B$。每次前向新写一个 token KV。remote 的基础远端部分按式 (18a)反复远读，目标持有的基础部分和新增部分本地读取。各来源总有效内容必须合回式 (18b)，同一 token 不能同时作为两个唯一来源重复计算。

实际 kernel 的 padding、query tile 重复读或当前 KV 片上驻留，由访问模板调整物理字节，不改变逻辑身份。prefill 扫描次数用完整 chunk 数及一个尾 chunk 闭式计算；不构造长列表。若 $a=\lfloor U/q_c\rfloor$、$b=U-aq_c$，则 $\sum\nu=a\lceil q_c/q_{tile}\rceil+\mathbf 1_{b>0}\lceil b/q_{tile}\rceil$。

这些闭式只在给定预测长度后对账精确，不表示预测长度正确。P 初始预测、边界预测与真实执行账分别保存，不能把两次对同一未来工作的预测字节相加成实际流量，也不能使用最终实测长度反填在线成本。

<a id="capacity"></a>

## 14. 容量与驱逐：做摘要估计，不做候选内 E 仿真

### 14.1 实际占用、预留和峰值分开

每 rank 容量账：


**式（19）**

$$
C_r=W_r+O_r+Z_r+F_r.
$$


W 为常驻权重，O 为已物化的实际占用，Z 为其他事务尚未物化的承诺空间，F 为可新预约空间。一次分配从 Z 转 O 时只转账，不再增加一次总量。

P 初始预测每层新增 KV 为 $(U+\widehat n_D)b_{g,r}$。D 边界时 U 已是基础 KV 的一部分，之后新增量为 $\widehat n_B\,b_{g,r}$，不能再把 U 作为未生成增量预约一遍。stay/copy/recompute 的 D 目标最终持有完整基础上下文和 D 增量；remote 的 D 目标持有已有本地区间、池恢复部分和 D 新增量，其他基础区间继续在其真实来源。

新增峰值加上实际 kernel 工作区、padding 和 remote 双缓冲。目标已有本会话可安全复用的实际分配，以及可合法转用的本请求未物化预留，分别扣除且只扣一次；不能扣其他实例即将释放的容量。P/D 切换过程中的源数据、旧确认尾部和新目标分配若同时存活，必须在各自 rank 上同时存在。


**式（20）**

$$
\Delta b_r=\max(0,\widehat B^{peak,new}_r-F_r).
$$


峰值对准备、prefill、decode、merge 的时间阶段分别检查。不同时间释放的缓冲和持久 KV 不机械相加成“全程同时驻留”；但没有真实释放事件时也不能提前扣除。

### 14.2 驱逐摘要的数据结构

每个实例维护当前**合法 inactive 对象**的可释放字节摘要：按既定 T 顺序涉及的有限类别、E 的易释放后缀/其余层，以及写回端口/路径归属分桶。同一桶的释放原子具有同一种逐 rank 分片比例；比例不同必须分桶，不能把本来耦合的 TP 分片拆成各 rank 独立选择 victim。

每桶保存逐 rank 总可释放向量 $R_{b,r}$、干净/脏字节向量 $D_{b,r}$、原子个数/最大原子向量，以及固定释放/确认服务模板。干净表示已有可直接复用的有效 backing，脏表示释放前必须写回；二者不是会话工具/人类类别。桶内仍按原有对象排序执行，摘要只做聚合估值。

摘要由完成、激活、引用变化和真实释放事件增量更新。候选查询不扫描所有 session，不逐 victim 调用 `k_hide`。E 的软目标使用最后一个合法在线规划结果；摘要对此是近似视图，实际提交仍按真实规则重新确认，不能把软目标视为永不变化的物理事实。

按固定类别顺序消费逐 rank 缺口向量 d。对桶 b，取 $\theta_b=\min\!\left(1,\max_{r:R_{b,r}>0}\frac{d_r}{R_{b,r}}\right)$；若该桶不能覆盖任何仍有缺口的 rank，则 θ 为 0。预计释放 $\theta_b R_{b,r}$，并更新 $d_r=\max(0,d_r-\theta_b R_{b,r})$。预计脏写回向量为 $\theta_b D_{b,r}$，预测事务数使用相同比例的原子数作启动费用近似。遍历一次各桶后仍有缺口，进入深缺口分支。

这是连续份额近似，不是真实释放计划。只有在桶内同分片比例且按确定对象前缀释放时，最后一次原子取整的超额才可用一个最大原子界定；摘要对 dirty 分布、对象顺序、启动次数和过期 E 软目标的误差不受这个界自动约束。不得把“最大原子”写成整个驱逐预测误差的数学上界。实际提交仍只能释放完整、合法的对象/层组。

### 14.3 释放何时完成

干净且无引用的副本只支付元数据释放费用；脏副本支付源 HBM 读、路径、池写及确认费用。对预计参与的逐 rank 写回流应用式 (6)-(7)，共享池端口自然竞争，再得到各 rank 的空间就绪时刻。不能用“最大 rank 缺口 ÷ 一个池端口带宽”代表任意布局。

如果合法 inactive 字节不足，使用当前已知 active 工作的因果结束估计形成深缺口等待；仍不知道什么时候可释放时，标记 `capacity_wait_uncertain`，使用本实例等效服务排空估计作为有限近似。**这不是可执行保证。**选中后无合法释放对象就挂到容量变化事件上等待，不能使用未来不存在的空间。

对从现有已知输入、权重、最小工作区就能证明空实例也放不下的情况，记录结构性不可执行；不能仅因为预测输出较长就将其当作真实失败。所有实例仍保留在审计表中。

### 14.4 不将未来 victim 恢复费用加到当前请求

当前请求只承担其真实前驱上的写回、空间等待和自己需要的历史恢复。被驱逐会话未来返回的损失，不属于式 (1)的终点；本版不引入这项外部性。E 自身可按照其既定目标运行，但不能将 E 的累计逐层偏移直接塞进当前 cost。


### 14.5 P→D 的阶段预约事务

边界先冻结本请求已有实际分配、未物化预留及已发义务。对目标 j 的预计峰值 $P^{D,peak}_{j,r}$，额外预约量为：


**式（20a）**

$$
Z^{D,add}_{j,r}
=\max\left(0,
P^{D,peak}_{j,r}
-O^{self,reuse}_{j,r}
-Z^{self,transferable}_{j,r}
\right).
$$


只有在相应使用时段仍有效、且不会被其他操作占用的 own 分配可以计入 reuse。其他请求的 O/Z 不是可扣项。真正还需驱逐的缺口，再用 $Z^{D,add}$ 减去目标当前可新预约 F；不要把已经在 F 中排除的 own 预留再次当作自由空间。

D 目标不同于 $i_P$ 时，源端仍被引用的 KV、未完 P 传输的工作区及确认资源不能随预约“搬走”。可取消的只是尚未物化、未被已发操作使用的默认 D 增长承诺；在同一个阶段提交事务中创建新目标承诺、转用同目标承诺、释放这些未使用旧承诺。实际 O 的释放仍需安全完成事件。

提交失败且尚未产生任何物理操作时，不破坏原地继续所需状态；等待状态变化或回到同一未提交边界重评估。驱逐/搬运已经提交后，D 方案锁定：即使第一步 D 尚未开始，也不能重新选点并丢弃刚刚造成的状态改变。原子化只要求逻辑账一致，不意味着长时间持有全局锁。

P 末尾到 D 决策提交之间，本请求基础 KV 必须保持阶段引用保护，不因“暂时没有活跃 D”就进入冷 victim 集合。D 每个源的引用与目标预留建立后，才结束不再需要的 P 阶段引用。远读缓冲可复用同目标的空闲分配；未消费或未确认空闲的块不能提前转用。


## 15. 轮末合并与位置更新

### 15.1 合并依据最终实际位置，不依据第一次 action 名字

若 D 结束时 $i_D$ 已有完整并集，例如 D 采用完整 copy/recompute，则只完成必要控制提交与旧副本释放确认，不重复搬回最初 home。P 的 action 不再单独决定最终合并。

D 采用 remote 时，基础区间可能分在最初 home、P 实例和 D 实例，最多三个片上位置。对每个位置 s，令 $V_s$ 为其**唯一有效、非重复的权威区间**保留量。不能让同一交接重复副本同时增加两个位置的 V。已有完整并集的一端直接作为结果位置；否则固定选择保留量最大的现有持有位置：


**式（21）**

$$
w=\arg\max_{s\in\mathcal O_{end}}V_s.
$$


同量时，优先边界逻辑 home $\omega_B$，然后当前 D 实例，最后实例编号。只有两处持有量时退化为少并多，并保留“同量留原逻辑 home”的规则。该规则不是以两条或多条路径的预测耗时最小值选 merge 目标。

所有其他持有位置只把 w 缺少的区间搬给 w，已有副本不重传。来源按稳定实例/区间次序发到配置允许通道，共享资源按第 9 节计算。不要做先两两合并再重新选最大者的顺序搜索，不新引入第四个合并位置。

### 15.2 合并时间与旧义务

在本候选的 `service_done` 投影占用上计算落点额外空间，不能用原始 $t_P$ 或 $t_B$ 占用再次预约已经持有的增量。已经真实释放的临时缓冲才可扣除。

设 $F^{payload}$ 为所有必要合并载荷完成时刻，每个传输都由 `service_done`、自己的源就绪、目标空间和通道前驱门控；$F^{old}$ 为其余必须闭合的已提交义务完成时刻。没有相应事务时取 `service_done`。则：


**式（22）**

$$
F^{merge}
=\max\left(F^{service},F^{payload},F^{old}\right)
+T^{commit}.
$$


源 ack 尚未结束不必阻止无依赖的其他合并传输，但最终必要确认不能漏掉。每次 payload 仍包含源 HBM 读、正确方向 NoC、目标 HBM 写。没有 payload 不代表控制提交一定为 0。

本版在本轮响应结束前不预搬尚未由协议确定的最终合并内容；P→D 为 D 服务发起的 copy 是阶段准备，不是提前猜测轮末 merge。初始和边界估计都用各自预测增量计算末态，实际执行用已发生字节，三者方向可能不同，必须记录。

### 15.3 容量回退与进展保证边界

目标 w 暂无空间但有可完成的合法释放路径时，等待该目标，不按路径速度改目标。只有运行时在第一笔 merge 载荷提交前确认当前承诺下无可完成进展，才按同一“保留量降序＋固定平局”次序检查剩余持有位置；每个最多检查一次，最多两次替代检查。两处布局即退化为只检查一次反方向。

候选中有合法目标就记录 `merge_capacity_fallback`、取得真实空间后执行；全部位置无进展则报告资源阻塞/结构性失败，不隐含转池、不无限重试。第一笔 merge 载荷已提交后，目标不再改变。

预测器使用相同目标次序，但容量摘要是不精确的。信息不足时给有限保守等待并标记不确定，不能由不可靠摘要断言真实可执行，也不能为了模型好看删除请求。

引用、预留和进展检查覆盖全部持有位置。若所有请求只等待容量且没有计算/在途事件可释放空间，触发停滞检测；有限位置数量和确定性排序不等于任意过载都能完成。

## 16. 两阶段的总时间与提交流程

### 16.1 初始预测与实际 P

在 $S_P$ 中为每个候选生成 P 的空间 A、数据 R、计算 Q 和层级服务，得到预测 P 完成；再按默认继续布局估 D 和最终合并。这里只形成一个完整预测，不在其内部调用第二次全实例搜索。

选择后实际提交 P。后端在最后 P 计算及首 token/停止状态可用时发出 `P_COMPUTE_DONE`。不得仅因为初始预测包含 D，就在同一个不可中断列车中预先提交本请求的 D；否则已经没有合法的第二次决策边界。

### 16.2 第二次预测的计算顺序

在 $S_B$ 中首先构造原地继续，取得共同 $\widehat n_B$ 和继承义务。其他候选从同一位置/区间快照建立自己的缺失量、空间与服务。D 只计新准备、必要的新重建、正常 D 前向和结算；源端已完成的 P 工作不重新开始。

第一步 D 用完整层级递推，此时基础上下文为 $H_B$，每步输入为一个已有 token；后续 $\widehat n_B-1$ 步使用固定区段折叠。recompute 候选先支付 H_B 重建再执行第一步 D，不遗漏也不重复。

第二次选出的方案只在包含新增控制费用后确实优于继续时改变。确认提交后建立 D 位置、获取所有来源引用和预约，并建立实际数据门；第一步 D 不能在必要数据/空间之前执行。与原地继续相比的预测节省不是从成本中另减的奖励。

### 16.3 未完工作和时间账

旧 P 义务通过真实 ID 只登记一次。已完成部分是沉没工作；剩余部分可能与新 D 准备/计算交叠，也可能成为最终尾部。不得强制把所有旧尾部串行加在 D 前，也不得在第二次决策时全部清零。

两次预测都不是两份实际执行。最终 E2E 来自真实事件 $F^{service}_{actual}-t_{arrival}$；不能把初始预测整轮成本与边界预测剩余成本相加。预测误差分别对各自时刻的剩余真实完成时间计算。

P 通常已产生首 token。D 迁移可能拉长首、次 token 间隔，必须记录 $t_{token2}-t_{token1}$ 及后续 ITL，而不仅报告平均 decode 速度。

### 16.4 单请求状态机

```text
P_DECISION -> P_COMMITTED -> P_RUNNING
    -> P_COMPUTE_DONE
        -> [已知终止] MERGE_PENDING
        -> [还需 D] D_DECISION_PENDING
            -> D_PLAN_SELECTED
            -> D_COMMITTED -> D_RUNNING -> MERGE_PENDING
    -> MERGE_COMMITTED -> DONE
```

`P_COMMITTED` 冻结已提交 P 操作，不冻结未提交 D。`D_COMMITTED` 的界限是第一项不可撤销的新阶段操作，例如驱逐、copy 发流或不可逆预约/引用转移，而非第一 token 算子启动。此后本轮 D 不再选点。

同一边界的快照失效重试只允许在尚无不可撤销工作时、由相关真实状态事件触发，并计入开销；它不是第三种常态决策。改变方案提交失败且无不可逆工作时，保留继续方案的合法状态。成功路径通常只进行 P、D 两次全候选遍历。

对正在取消/终止的请求，不发新 D；已有传输仍完成必要排空、释放和错误记录。P 边界不能同时被两个回调处理，使用阶段 epoch/idempotency key 保证一次提交。

<a id="complexity"></a>


## 17. 算法、复杂度与内存约束

```text
# 初始：每个候选只估一种默认 D 延续，不搜索其他 D 位置
S_P = readonly_snapshot(stage="P")
horizon_P = causal_decode_steps(S_P)
for i in all_instances:
    for a in four_actions:
        result = estimate_P_and_default_continuation(S_P, i, a, horizon_P)
        record_compact(result); update_best_initial(result)
commit_P_only_after_version_check()

# 由真实 P 完成事件触发；第一笔 D 尚未提交
on_P_compute_done(request):
    if observed_terminal(request): enter_merge(); return
    S_B = readonly_snapshot(stage="PD_BOUNDARY")
    horizon_B = max(1, causal_decode_steps(S_B))
    continuing = normalize_current_continuation_into_four_actions(S_B)
    for j in all_instances:
        for a in applicable_four_actions_at_boundary(S_B, j):
            result = estimate_remaining_D_and_merge(
                S_B, j, a, horizon_B, inherited_obligations=S_B.pending)
            record_compact(result)
            update_best_prefer_continuation_on_equal_cost(result)
    commit_D_transaction_once_after_version_check()
    # 后续 D 只进行实际资源执行、扩容或等待，不再做位置/动作搜索
```

原地继续复用对应的 stay/remote 记录，不能额外重复算成第五动作。每次最多 $4N$ 个 `(instance, action)`；正常需要 D 的请求至多 $8N$，已知在 P 后终止的请求只需初始遍历。**不枚举 $16N^2$ 个 P/D 组合。**事件触发的失败重试另外记录，不能用这个正常路径上界隐藏任意重试成本。

共同算子与整数工作量只计算一次或按少数动作语义建模板。两个阶段读取各自新快照，不缓存已经陈旧的动态 cost。静态路由与 TP 模板可复用，D 来源是已发生的有限位置集合。

令 K 为最多 8 个重复区段，h 为平均路由长度，B 为容量摘要桶数，$T_{TP}$ 为配置 collective 模板的处理量，$S_{max}\le2$ 为正常布局下目标外的片上基础来源数。单决策目标复杂度为：


**式（23）**

$$
\mathcal C_z=O\!\left(
C_{snapshot,z}
+4N\left[p(S_{max}h+KL)+Bp+K T_{TP}\right]
\right).
$$


桶保存逐 rank 向量，所以桶查询为 $Bp$，不能把 rank 遍历藏掉。有限 pool 路径、最多三个合并位置及固定控制段已包含在常数中。正常整请求的总预测费用为 $\mathcal C_P+\mathcal C_B$，而不是两者相乘：


**式（23a）**

$$
\mathcal C_{request}
=O\!\left(
C_{snapshot,P}+C_{snapshot,B}
+8N\left[p(S_{max}h+KL)+Bp+K T_{TP}\right]
\right).
$$


D 的一次 H_B 重建仍使用首 chunk 递推和最多 8 区段，不逐 token 展开，因此不改变上述形状。初始布局若扩为任意多源、每层路由不同或区间碎片无界，必须显式增加来源、区间和路径遍历项，不能继续宣称 $S_{max}$ 为固定常数。

在线临时内存使用一个可复用 $O(pL+S_{max}ph+Bp)$ 工作区及 $O(N)$ 候选摘要。D 评估顺序执行，不复制每个候选的 P 终态；边界只存在一个已经发生的真实终态快照。系统级 KV/流元数据按真实在用对象数增长，另报，不声称全系统常数空间。

通道最早空闲查询若通道数不是小常数，另加 $O(pL\log C_{chan})$；每层路由不同则路径项扩大。每次快照获取、摘要维护、边界事务和完整 E 实际规划的费用都要计入仿真总开销，不能隐藏在接口里。

日志流式保存阶段标记、选中项、必要分解和异常；不在内存堆积全部候选逐层轨迹，不按 $H\times L$、$\widehat n_B\times L$ 分配数组。本版不引入候选内完整 E 递推、置信区间剪枝或自动多级精化。

### 17.1 后端仿真也必须有界，不能只让预测器快

上面的复杂度只针对在线计价，不等于真实执行需要处理的数据量变成常数。尤其把 remote 改成字节块后，真实块数可能增加；必须避免为整轮 `步数×层数×块数` 提前生成节点。模拟 HBM 只保存字节计数、区间身份、引用和完成依赖，不申请与模拟 KV 字节等大的宿主数组。

执行端为每个实际在用 remote 流保留有限状态：当前来源、层/query tile、剩余块数、尾块字节、两个缓冲的占用和完成门。D 最终选中前不得预建或提交本请求原位置的 D 事件；其他请求的列车/流不必因此暂停。按缓冲/通道上限延迟发出下一块。copy 和 pool 恢复也按真实通道数有界发射，已完成的临时节点在依赖消费后释放。宿主工作区随活跃流/已提交依赖增长，而非随整个未来轨迹长度增长。

延迟生成只限制内存，不自动减少总事件数。若事件数仍不可接受，可在“当前资源份额不变、没有外部到达/完成/仲裁变化、内部相同块重复”的区间内精确合并连续服务；合并遇到下一真实外部事件必须停止，并正确更新两个缓冲状态与已服务字节。这属于后端等价加速，不能把在线预测器的冻结近似直接当成真实执行时间。先用小例子逐块执行对拍，不能为跑快丢弃共享干扰。

若后端不支持上述等价批处理，须实际测量块事件量和 wall time；本版不承诺仅凭一个闭式 cost 就保证整个仿真快。tile 字节和通道数来自合理的执行配置，不为减少仿真事件而把容量暗改为无限。

## 18. 近似边界与正确性边界分别列清楚

| 可以近似、必须测误差 | 不能为了速度省略或伪造 |
|---|---|
| 在线输出长度均值 | 读取真实未来输出 |
| 等效串行队列等待 | 把队列长度直接当 ns |
| 阶段冻结的资源份额 | 同一 HBM 同时给多个流完整带宽 |
| 层内 copy 与计算重叠的保守忽略 | 数据未到就完成消费 |
| 固定 8 区段的时间积分 | 用采样值代替真实字节守恒 |
| 驱逐摘要中的对象顺序和整组超额 | 释放活跃/在途引用的 KV |
| 预测 merge 方向与实际方向不同 | 未计目标 HBM 写、未检查落点空间 |
| 动态背景 ETA 的近似 | 偷读后端未来完成事件 |
| 初始默认延续与边界真实状态之间的差异 | 为每个初始候选虚构最有利 D 终态 |
| D 接续/新接纳等待估计 | 原地请求重排队或异地请求免费插队 |
| 阶段切换未完尾部的 ETA | 清除已提交操作、重复计费已完成 P |

如边界改变，升级 `model_semantics_version`，旧实验不自动代表新模型。不能只修改注释而把旧数值当作新物理语义。

## 19. 必需的验收，不扩展研究问题

### 19.1 公式与守恒测试

至少检验：零历史/零传输；TP 不均匀和 KV 头复制；单跳多跳；共享/独立池端口；copy 层间迟到；remote 双缓冲及多来源/尾块；长上下文；短/长 decode；干净/脏驱逐；source 引用保护；二处/三处 merge、平局与容量回退；实际增长超预测；token 与前向步数关系。

P→D 专项必须包括：原地 remote 不变成本地 hit；回 home 仅补 U；第三目标来源齐全；D recompute 不重复输出/采样；首 token 已终止时不选 D；控制费用使小收益迁移不再合算；原地接续不重排队；P 的未完 ack/引用不消失；新目标容量提交失败仍保持旧方案可继续；第一笔新物理工作后不再次选点；旧/新增长预留不重复计入；P 末尾不预发 D；同一回调不提交两次。

### 19.2 同状态对照

从同一个真实状态分别执行适用候选，预测器不知道将来的执行结果。报告时延误差、排序错误及：


**式（24）**

$$
loss=T_{selected}-\min_{x\in\mathcal A_{measured}}T_x.
$$


测过哪些候选，就只声称哪些候选中的最优。初始比较来自相同 $S_P$，边界比较来自相同 $S_B$，不能把两种状态的实测混排。未执行项真值为 NA。若选择相同而绝对误差不同，不能包装成选择收益。

另外做一次映射对照与两阶段版本的闭环比较，保持相同初始模型及物理能力；记录 D 改点率、同址改动作率、交接字节、首/次 token 间隔、E2E、完成率与控制开销。边界局部最优不等于全局 P/D 组合最优，也不等于整体系统收益已经得到保证。

### 19.3 仿真可行性

在同状态、固定动作序列下测预测器自身 wall time，另测策略改变后整个后端的 wall time/事件量。记录每次决策 P50/P95/P99、总占比、峰值 RSS、摘要维护费用及日志大小。原有实际 E 规划成本也计入整机仿真，不因移出 cost 函数就忽略。

建议首轮工程目标为：模型升级及正常两次决策的额外宿主总时间，约不超过原固定动作回放的 10%；这是待验证预算，不是已达到的结论，不能由 $4N$ 变 $8N$ 就推定总仿真只慢固定倍数。分别测新增 D 评估、边界控制、阶段预约、真实交接和日志维护费用；若超预算先分析实际热点。扩展 N、p、L、上下文、总轮数及失败重试，确认无不必要的平方增长与完成请求长期保留。

宿主 Python 耗时不自动等于真实片上决策时延。论文若声称可在线部署，需独立说明部署位置、输入获取成本及实现预算。

## 20. 第一部分的最终设计裁定

**初始映射按默认延续估整轮，P→D 边界按真实状态重新比较剩余工作；P/D 可异址，原地继续始终参与，D 提交后不再改点。两个决策共用逐 rank 资源、逐层数据/空间就绪与统一 merge_done；长重复段固定预算，E 通过释放摘要进入候选，不进行 P/D 笛卡尔积搜索。**

这一版足够清楚地表达四动作差异，同时不把预测器做成第二套事件仿真。模型是否足够准确，由受控选择损失与端到端结果证明；文档本身不保证 ISCA 接收，也不宣称模型每一项预测精确。

---

<a id="part2"></a>

# 第二部分：与现有代码逐项对照

本部分只用于定位差异与修改要求，不反向覆盖第一部分的目标设计。


## 21. 对照版本、证据范围和修改类型

对照仓库：`cs-sunH/template`。本次重新读取的 `main` 为：

```text
304d8e8d844b1ab65bb686ffa63bdb6053c12b9d
提交时间：2026-09-26 01:25:22 UTC
```

C01—C14 保留原设计稿在该固定 commit 下的静态核读结论；本版重新核对 main 仍指向该 commit，并增加 C15—C16 的直接读取。累计核读范围包括 `joint_cost_model.py` 的核心估价、传输、容量、长度与遥测分支；`joint_scheduler.py` 的全候选选择；`event_recursion_predictor.py` 的 k 枚举与记录；以及容量预约、传输登记和构图器中的相关片段。没有编译仓库、执行仿真或逐行审查全部 C++ 后端；因此，下面不会将代码静态差异写成已经测出的速度、误差或正确性结论。

**三种修改要分清楚。**

- **计价修改**：实际工作不变，预测方式改变。例如补计 merge 目标写带宽。
- **协议/语义修改**：实际执行也改变。例如完整历史重算和有限字节双缓冲。必须两端同时落地，并重新生成实验；不是只改 cost 数字。
- **等价实现优化**：数学结果保持不变，只减少开销。例如用 sum/count 代替完整历史列表。

相较本文不同，不自动等于旧代码有 bug。下文明确区分“现有设计的近似”“本次主动改变的语义”和“已核实缺失的计价资源”。证据索引 C01—C16 在文末，每项链接固定到上述 commit 的对应源码段。

### 21.1 总览

| 编号 | 事项 | 处置 | 修改性质 |
|---|---|---|---|
| D01 | 全实例四动作 argmin、因果输入 | 保留；增加阶段参数和 D 平局优先继续 | 主体保留＋调度扩展 |
| D02 | 阶段整体 max 合成 | 改为逐层数据/空间就绪递推 | 计价修改 |
| D03 | Q 与准备时间直接相加 | 统一时间基准，允许已实现的排队/准备重叠 | 计价＋时序接口 |
| D04 | 池路径只有池端口项 | 补齐逐 rank 路由和 HBM 端点 | 必须补计 |
| D05 | merge 缺目标 HBM 写腿 | 加真实写服务，不用空间等待代替 | 必须补计 |
| D06 | 候选计算与候选传输的 HBM 自竞争 | 共用阶段份额 | 计价修改 |
| D07 | prefill/remote 扫描次数 | 从真实 chunk/query tile 推导 | 计价＋执行核对 |
| D08 | decode 为固定单 token 时间乘步数 | 增长上下文的有界分段服务 | 计价修改 |
| D09 | remote credit 与容量 | 字节双缓冲、有限占用、消费后归还 | 协议修改 |
| D10 | 重算按缺失层比例折 token | P 重放 H+U；D 只重建 H_B，再做剩余前向 | 明确语义改变 |
| D11 | 容量足迹和驱逐粗估 | 分阶段峰值＋干净/脏、端口摘要 | 计价＋状态接口 |
| D12 | 两处 merge 与原始 load 容量 | 末态投影＋至多三处确定性合并 | 计价＋协议扩展 |
| D13 | 全局瓶颈除数广播、吞吐倒推流数回退 | 每路径资源份额；规范流数来源 | 计价修改 |
| D14 | 保存输出长度完整列表 | sum/count；步数语义明确 | 等价优化＋接口核对 |
| D15 | E 多 k 递推及全部 trace | 不进入候选主路径；压缩诊断记录 | 开销边界 |
| D16 | 真实缓冲/交接/增长/死锁处理 | 必须与新模型协议一致 | 运行时联动 |
| D17 | 分解字段、取整、测试与总纲 | 升级 schema、向上取整、两阶段验证 | 工程与证据 |
| D18 | P/D 同实例锁定与单阶段提交 | P 完成处引入一次剩余成本选点，取消强制同址 | 调度/执行协议修改 |
| D19 | 单 home 视图、整轮工作与统一队列负载 | 当前区间布局＋D 工作/长度/接续视图 | 状态与计价接口 |
| D20 | D 预约移动拒绝、阶段引用及边界构图 | 新阶段事务、引用交接、只构建选中 D | 运行时联动 |

## 22. 逐项说明怎么改

### D01：保留全候选选择，不重写成新的优化器

**现状。**`select_instance_and_action()` 遍历全部 instance 和四动作，对适用项按 `(cost_ns, order_key)` 取最小；本身没有指数级组合搜索。[C07](#c07)

**修改。**保留全实例枚举，初始 tie 不变；D 边界同成本优先原地原方案继续，再按实例和动作序。`estimate_action` 增加阶段和当前布局参数；公共工作量与静态路由同决策共享。每阶段最多四动作记录，继续标记归并到对应记录，不重复算第五动作。候选只保存紧凑结果。

**验收。**等价瘦身时初始选择保持；启用阶段扩展后按新边界规则验证 D 的继续平局。两个时刻的所有实例都有审计项，不能只保留最近/最空闲实例。正常请求候选规模为两次线性遍历，而非位置组合平方。

### D02：从三条最终分支，改成同一种层级完成时间递推

**现状。**`estimate_action` 最后分别采用 remote 两阶段 `first_credit + max(...)`、copy 的 `max(tail,compute)` 和普通准备/计算合成。[C04](#c04) 这已经考虑流水，并非简单全串行；但仍将整段计算和整段数据时间压缩在少量标量内。

**目标。**输出 `ready_data[g,r]`、`ready_space[g,r]` 和固定数量算子段的服务量，再按式 (13)递推。copy/pool 的一次性传输只影响真实消费它的层。remote 用同一层前/后段，只替换 attention 数据供给服务。

**修改位置。**重构 `JointCostModel.estimate_action` 的“历史准备—计算—关键路径合成”三段；新增小型、纯算术的 `estimate_first_forward` 和 `estimate_repeat_segment`。这些名字是建议的新接口，不是声称已有函数。

**验收。**第二层晚到的 110 ns 例子通过；prefill 结束前所有实际需要层都已就绪；不存在通过整体 max 跳过数据到达后计算的情形。

### D03：Q、驱逐和准备使用同一时间轴

**现状。**当前最终分支均将 `target_wait` 加在前面；remote 再加 `eviction_wait`，copy/stay 采用某种准备与驱逐的 max。[C04](#c04) 是否能重叠，由不同分支的标量拼接隐含决定。

**目标。**Q 只决定计算何时可用；A 决定目标何时有空间；R 按源有效性和目标空间计算。能提前发出的准备工作从 0 时刻开始估计，而不是先支付 Q。不能提前的部分用真实前驱门表达。

**修改位置。**`target_wait_ns` 保留为观测字段，但不再被所有动作机械前置相加；容量接口从“一个附加等待量”升级为相对于本次决策的空间可用时刻。在线调度/构图必须提供“准备实际从哪个 frontier 发出”的说明。

**验收。**Q=100 ns、空间和准备在计算前已经可用的例子，不再重复支付准备时间；依赖驱逐后才能写目标时，不能用 max 把尚无空间的写入提前。这里需要核对实际发射顺序，不能仅改预测器制造并不存在的重叠。

### D04：补全片外池路径，同时补端点登记

**现状。**`_pool_transfer_ns(total_bytes, divisor, rates)` 只有池时延和池端口带宽项，没有接收源/目标 rank 或 Mesh 路径。[C02](#c02) 在线 `_register_transfer_flows` 对 pool 流有链路及池端口登记，但仅 `noc_migrate` 注册 HBM 端点；注释明确将池路径端点留在旧边界之外。[C11](#c11)

**目标。**每笔恢复/写回携带真实 edge、源/目标 rank、路径、字节和端点服务。用式 (7)在共同资源模型中估价。

**修改位置。**用逐 shard 的 pool transfer 接口替换聚合 `_pool_transfer_ns`；扩展 `_pool_port_divisor` 或直接按流查询实际 edge，而非用一个实例级最大除数替代所有端口。为 `remote_load/remote_store` 登记其真实 HBM 端点，并在真实完成事件注销。

**验收。**相同恢复量落到远/近实例的路径代价有区别；两个 rank 共享一个池端口与使用两个独立端口不同；恢复写和本地消费共用目标 HBM 时，吞吐总和不超过该端口服务能力。

### D05：merge 的目标写服务不能用容量准备代替

**现状。**`_shard_leg_ns` 仅在 kind 属于 `read/copy/staging` 时加入目标 HBM 写项，kind=`merge` 不进入此分支。[C02](#c02) 在线传输登记中，`noc_migrate` 的两个 HBM 端点却都会被登记。[C11](#c11)

**目标。**合并传输与 copy 一样支付源读、NoC、目标写；落点空间准备是另一个前驱，不等于写带宽成本。

**修改位置。**使 merge 消费共同传输原语；保留正反方向独立路由。控制确认若真实存在，计入 metadata/commit 段，不因为没有 payload 就假定全部零时间。

**验收。**目标 HBM 很慢而 NoC 很快时，merge 完成时间必须反映目标写瓶颈。容量充足不应令这项服务消失。

### D06：算子与候选流在同一个 HBM 份额模型下

**现状。**传输端点使用 `u_home+1/u_exec+1`；计算则用 prefill/decode 基础时间乘在线因子。[C02](#c02)[C03](#c03) 这些接口尚不是本文所定义的“候选传输＋候选计算”共同阶段份额。现有在册后台流与活跃 decode 统计可以保留。[C12](#c12)

**目标。**同一阶段先建立候选逻辑流集合，再同时产生传输速率与计算 HBM 速率。计算读、copy 写、pool 写和 remote 暂存读写不能分别拿到完整端口。

**修改位置。**给本地算子求值器提供候选阶段的 HBM 份额；现有 `HbmPortFlowRegistry` 继续描述已经在册的工作，另加本候选临时占用，不能污染真实登记表。阶段结束后只改变预测视图，真实状态仍由事件更新。

**验收。**单个 HBM 端口上两个长期满载候选流的估计总服务不超峰值；将 copy 尾流隐藏在计算后，仍能看到它造成的共享资源减速。

### D07：保留“只远读源端基础历史”，补齐扫描次数

**现状。**当前 remote 已经使用 `history_bytes_by_tp_rank × (prefill_scans+decode_steps)`，不再把执行端新增 KV 全部乘进远读量。这一点与本文一致，应保留。[C03](#c03)

`_remote_read_passes` 允许外部传入 prefill 扫描次数；未传时只要有输入就取 1。[C05](#c05) 因而不能仅看 helper 就声称生产路径一定漏算多 chunk，必须检查调用方是否一直正确填了该字段。

**目标。**扫描次数由已知 U、$q_c$、`q_tile` 和 kernel 模板现场推导，采用式 (11)(18)，而不是依赖来自将来执行或未说明含义的数字。

**修改位置。**`RequestView` 增加 chunk/query tile 的合法模板身份，`_remote_read_passes` 改为推导；对已有 `prefill_scan_passes` 输入进行一致性校验。所有 remote 字节计算继续只使用源端保留历史。

**验收。**将 U 从一个 chunk 增为多个 chunk，基础历史远读量按扫描次数增长；保持 U 不变、改变 query tile 时读量与执行一致。输入/生成增量仍不错误重复跨 Mesh。

### D08：对上下文增长求服务，不将所有 decode 步视为一个固定价格

**现状。**JCM 内 decode 计算为 `estimated_decode_tokens * decode_ns_per_token * decode_factor`；prefill 可调用整段 context-aware 函数。[C03](#c03) 单 token decode 服务函数如何在调用侧选择其代表上下文，本次没有穷尽所有构造调用，因此不把它断言成固定在某个错误上下文。

**目标。**共同算子模板显式接受当前上下文；按式 (16)处理剩余前向，字节仍用整数闭式核账。第一步/第一 chunk 的恢复启动与后续稳态分开。

**修改位置。**将仅有 `decode_ns_per_token` 的接口扩为按上下文求值的固定成本模板；不在 estimator 内循环 n̂_D 次。prefill 的长 chunk 序列也遵循相同有界规则，避免其注入函数在每个候选里重复长循环。

**验收。**长上下文与短上下文的 decode 服务有合理差异；n̂_D 增大几千倍时，预测器调用次数仍受 K=8 限制；物理 KV 读量准确守恒。

### D09：remote 的字节双缓冲是协议修改，不是改一个系数

**现状。**JCM 的 credit 大小通过 `remote_credit_block_size(..., read_passes)` 产生，再以“基础历史 × credit_steps”切首块/尾流；源码注释明确说明 credit 在途字节仍不进入执行端 HBM 容量，暂存归还没有实际容量动作。[C03](#c03) 已读取构图器的 prefill remote 分支同样注明读流不物化入容量账。[C13](#c13)

**目标。**采用第 12 节的有限字节块、两个真实暂存区和消费后 credit 归还；执行和预测都遵守相同规则。真实数据流仍是源历史重复读取，不改变计算语义。

**修改位置。**`joint_config` 的 credit 参数升级为字节 tile/缓冲配置；JCM 增加缓冲足迹和双缓冲闭式；`reserve_request_capacity` 增加暂存预约；`graph_batch_builder` 的 remote 生产/消费门增加真实缓冲复用依赖；完成/异常清理必须注销缓冲。旧“按多步历史体积切 credit”实验不能直接充当新协议结果。

**验收。**任意时刻实际远读暂存不超过 2 个块；慢计算不能导致源端无限泵出；1 块、2 块、尾块与跨层复用均守恒。不要通过将模拟 HBM 的字节数真的分配成宿主大数组来实现容量约束。

### D10：完整重算是本次重新定义，不是把旧折算式证明成正确

**现状。**`_recompute_missing_tokens` 在驻留目标使用 `ceil(H×missing_layers/L)`，异地使用 H；prefill 历史基数也按是否驻留分支决定。[C05](#c05)[C03](#c03)

**目标。**P 初始 recompute 为完整 H+U 重放；D 边界为 H_B 的静默重建，加上正常剩余 D 前向。两种重建的初始历史基数均为 0。旧 KV 不替代所需层间激活；D 重建不重新采样已输出 token。

**修改位置。**替换 `_recompute_missing_tokens` 与 `history_base` 选择，增加 `rebuild_context_tokens`、`suppress_sampling/output` 和阶段字段；在线工作描述、token/RNG 元数据与构图器同步。D 不把旧 U 再作为新 prefill。容量可复用安全的本地旧分配，但不免除 FLOP。

**验收。**LOCAL/PARTIAL/REMOTE、驻留/异地四组情况下，重算的 token 序列与算子工作都有一致的解释；旧部分层折算的回归值需要按新执行语义重写，不能为了旧数值一致而继续使用本版已取消的折算。

**边界。**这改变了动作策略。若未来用真正 checkpoint 做部分重算，那应给出另一份完整数据依赖设计，不能同时把两种语义都称为当前 recompute。

### D11：空间预测从“一个 KV 向量”升级为分阶段足迹与释放摘要

**现状。**`_space_footprint_by_tp_rank` 已保留输入逐 rank 分布，这是正确基础；remote 的足迹是增量或后缀＋增量。`_eviction_wait_estimate` 用最大 rank 缺口估池写回，并对深缺口做有限等待修正。[C05](#c05) 已读 `reserve_request_capacity` 以 KV 基础分片及后缀分片预约。[C09](#c09)

**目标。**加工作区、有限缓冲、真实交接重复量和分阶段峰值；清楚区分 O 和 Z。驱逐摘要区分干净/脏、实际 edge、合法释放类别及原子大小。 摘要按逐 rank 向量及相同分片比例分桶，用同一个 θ 释放一个桶的耦合分片，不能让不同 rank 独立选择不存在的 victim 组合。

**修改位置。**新增 `PhaseFootprint` 与 `CapacitySummary` 输入；`KVCacheManager` 在真实状态改变时维护摘要；候选只查询。实际 `_ensure_capacity` 的 T/E/victim 决策继续保留，不挪进候选循环。[C10](#c10)

**验收。**相同总 KV 但不同 rank 不均匀布局，会在正确 rank 产生缺口；干净对象不付不存在的池写回；池热点按实际共享计价；摘要查询成本不随所有已完成 session 的累计数量增长。


### D12：从两处合并扩为最终布局上的确定性合并

**现状。**JCM 的已读分支只比较 home 与执行端，实际方向按保留字节量而非两方向 min 时间决定；空间估计使用现有 loads。[C04](#c04) 这可保留为二处退化情形，但不能覆盖 P/D 分开后至多三处的正常布局。

**目标。**第 15 节以 D 结束的实际/预测区间 owner 为准，已有完整并集则留该处，否则聚到唯一保留量最大的持有位置。相等优先边界逻辑 home，再 D 实例和编号。目标空间按 service_done 投影占用求额外量。

**修改位置。**把 home/exec 两个标量升级为至多三个位置的摘要及唯一内容去重。`project_end_state` 只保存候选对有限位置的差量；`merge_from_layout` 返回固定目标、各来源缺失区间、通道/引用义务及目标空间，不搜索目标速度最优。容量回退按固定保留量次序检查尚未尝试的持有位置，最多两次，载荷提交后锁定。

**验收。**两位置退化为既定少并多；三位置的结果不依赖人为两两合并顺序；重复副本不增加 V；已在目标的区间不重传；P 阶段 action 不覆盖真实 D 末态。旧确认尾部与合并可按依赖重叠但最终都闭合。

### D13：保留流数通道，去掉错误来源回退与过度聚合

**现状。**`TelemetryLinkFlowView` 已优先使用 `active_flows`，也按旧流的登记/遥测去重后再加候选占用；应保留。[C06](#c06) 缺流数时仍有 `capacity/rate` 的回退。传输原语对候选全部路径取一个并集瓶颈除数，再给各 shard 使用。[C02](#c02)

**目标。**总体吞吐不再冒充流数；无覆盖就显式报未知并使用在册流近似。对各条路径读取其实际资源份额，避免不经过热点的 shard 也被统一热点除数减速。相同路径/阶段的查询可以共用结果。

**修改位置。**规范 telemetry 字段语义；替换单个 `priced_divisor` 为小型、只读的每资源/每路径服务视图。在线 `_register_transfer_flows` 已有逐 shard 完整路由，可复用其位置身份。[C11](#c11)

**验收。**只有一条路径拥塞时，不相关路径不被错误减速；旧后台流与候选自身不重计；流数缺失不会通过“总吞吐满载”推断成一条后台流。

### D14：长度估计和在线因子，分开处理

**现状。**`CausalHorizonEstimator` 为每会话 append 完整列表，再 sum 求均值。[C01](#c01) 现有 `ServiceFactors` 采用按时间差/上一服务时长决定的更新系数，不是本文冻结的 1/16。[C01](#c01) 在线侧已说明 `transfer_factor` 是观测/披露通道，不直接进入 `estimate_action`，避免了与竞争除数的重复计价。[C12](#c12)

**修改。**长度列表换为 sum/count，保留整数结果与版本号；两个阶段都用前向步数，边界未终止时至少 1 步。P 完成不作为一条已完成请求样本，边界更新不重置已采样 token/RNG。在线因子可迁移为本文固定 EWMA，但这是更新规则改变，不是旧规则错误；所有新实验从同一定义的冷启动重新开始。不要把本来只用于披露的 `transfer_factor` 又乘进新带宽。

**验收。**同一已完成长度序列下，sum/count 与旧均值逐次一致；同一会话一百万轮不会保存一百万个历史长度；资源竞争已计价后，效率因子不再学习同一拥塞。

### D15：E 不作为全候选内核，详细记录也不长期保存

**现状。**`predict_k_hide` 枚举 k 并运行 `_trace_for_k`，保存 `all_traces`；每个 trace 有逐层 timing。`compute_slowdown_ns` 累加各层完成偏移。[C08](#c08) JCM 的驱逐估计目前没有直接调用这个完整预测器。[C05](#c05)

**目标。**保留 J/E 的轻量接口边界。正常 E 运行只需保留选中方案及摘要；详细失败 k 轨迹按验证需求流式记录。若某个指标要表示请求总延迟，应使用最终完成时刻差，不能用逐层偏移之和。

**修改位置。**给 `predict_k_hide` 增加“不保留所有 trace”的诊断控制，保证只改变记录、不改变所选 k；将累计偏移字段改名为明确的累计指标，另给 `end_to_end_slowdown_ns`。**不要把本文的简单层递推误接成“每候选调用一次 predict_k_hide”。**

**验收。**关闭详细记录不改变 E 决策；尝试多个 k 时临时内存不保存 O(L²) 项；一个早期 10 ns 停顿不会被作为请求总延迟重复加 L 次。

### D16：执行图已经有层门，不能将它说成全缺失；需要的是协议对齐

**现状。**已核读的 `graph_batch_builder` 具有 copy 尾块旁支、prefill remote 读旁支、后缀恢复组和按层消费的完成门；后缀恢复还存在组间并行路径。[C13](#c13) 因而不能声称“代码没有数据就绪机制”，差别主要在 JCM 没有按相同粒度计算。

**修改。**复用真实层/组边界和已有数据门。当前可并行的恢复组，不应为了模型公式简单就被无条件改串行；第一部分式 (12)按实际通道配置解释，多通道分别递推并在资源份额上汇合。

有限 remote 缓冲、重算工作 span、实际增长超预测、块交接后容量更新，以及本文明确的 merge 等待/容量回退规则，需要与管理器和构图器共同落地。这些变化不可能仅修改 `joint_cost_model.py` 就完成。

**边界。**本次没有逐个检查这些模块的所有完成/异常回调，不能据已读片段认定全部路径已守恒。特别是“代码中先减少逻辑占用，后端靠前递依赖保证写回完成”的实现，必须结合真实事件和预留账检查，不能仅见某个 `_remove_local_shards` 调用就武断判定提前释放。[C10](#c10)

**验收。**真实 producer/write/consume/release 时间闭合；未完成写回的空间不能被物理消费；异常后引用与预留全部可解释。后端采用有界活跃流/延迟生成，不能为整轮所有 token×层×tile 提前构造大型事件图。必须在本请求的真实 P→D 边界停下构图；未选出的 D 位置不能被预先物化为实际节点。

### D17：字段、整数时间、文档和新旧实验身份

**现状。**当前 `ActionCostBreakdown` 以少量标量及 notes 披露，`cost` 通过不同分支组合；传输函数多处 `int(bytes/rate)` 向下截断。[C02](#c02)[C04](#c04) 根目录总纲仍含远读终态上下文和旧整体合成表述，与当前源码的基础历史读量及两阶段实现已有不同。[C14](#c14)[C03](#c03)

**修改。**新增 `model_semantics_version`、`decision_stage/epoch`、`prefill_instance/decode_instance`、`prefill_action/decode_action`、`is_continuation`、`estimated_remaining_decode_steps`、`predicted_next_token_done`、`predicted_service_done`、`predicted_merge_done`、当前区间布局、继承义务 ID、phase 字节/空间、缓冲峰值、近似状态及固定配置 manifest。真实时间与预测字段分开；非执行候选真值为 NA。需要等待的服务向上取整，零字节不发生事务则严格为零。

在同一份总纲中同步更新动作语义和预测边界。保留旧实验文件及旧 SHA，但不能重命名为新协议结果。新增/调整对应单测，不按旧数值逐位一致来阻止真正的语义修正。

**验收。**可以从日志重建一次候选计算采用的公式版本、输入来源、字节/空间以及关键等待；不能通过把预测值再写成“实测值”来验证自己。


### D18：取消强制 P/D 同址，增加一次有真实边界的 D 决策

**现状。**现有选择入口做整请求全候选选择。[C07](#c07) `move_request_capacity_reservation` 明确把 joint 的跨实例 D 预约视为不支持并抛异常。[C15](#c15) 因此“调用一次原 argmin 再改 instance 字段”不能视为已支持 P/D 分开。

**目标。**初始成本按一个默认 D 延续方案估整轮；真实 P 完成、D 未提交时，用当前快照评估剩余成本。默认继续始终存在，并在相等时优先。正常不做位置对组合搜索，也不在 D 中继续调点。

**修改位置。**新增 `estimate_P_with_continuation` 和 `estimate_D_remaining` 的逻辑入口，仍共用轻量资源/就绪内核；在线调度增加 `P_COMPUTE_DONE -> D_DECISION_PENDING -> D_COMMITTED`。P 完成回调、train drain 处理、D 加入 batch 与完成归属必须同步更新，不能把旧同实例快捷分支留成另一条未审计通路。

**验收。**初始候选不调用全 D 搜索；D 在真实边界只做一次正常全遍历；D 提交后无第三次位置决策；边界已终止时跳过。已完成 P 不再收费，D 新迁移不回填为 P 期间重叠。此项是实际策略扩展，不是注释修正。

### D19：D 候选从当前布局和阶段工作生成，不复用到达时的 SessionKVView

**现状。**已读 `RequestView/SessionKVView` 使用初始 input/history、home/resident 等字段，remote 字节以原 history 向量与遍数计算。[C03](#c03)[C05](#c05) 当前这些字段足以描述旧同址范围，不等于能够表达 P 增量在另一个来源、D 再到第三处的区间布局。

**目标。**增加 `BoundaryKVLayout` 与 `StageWork`：H_B、按层/token/rank 的有限来源区间、有效副本/权威归属、已承诺接收端、首输出 pending token、终止/RNG 状态及旧义务。D 远读只数对当前 j 实际在远端的基础区间，D copy 只补目标缺少部分。

**修改位置。**构造 D 专用因果 view，长度入口归一为剩余前向；等待接口分为原地接续与新接纳。同一已有请求不能被移到新请求队尾来制造迁移收益，也不能给异地免费 batch 接入。多源 remote 仍使用同一对有限缓冲，来源边界排空，不生成任意块布局组合。

**验收。**home H、P U 的例子分别正确得到：P 继续远读 H；回 home copy U；去第三处 copy H+U；第三处 remote 反复读取两个实际来源。D recompute 静默重建 H_B，正常第一步仍只消费已发首 token 一次。

### D20：跨阶段预约、引用与实际构图必须新建事务，不只是删拒绝断言

**现状。**`move_request_capacity_reservation` 中跨实例路径直接拒绝，并明确告诫旧逻辑不能静默复用；其下的 `decode_hbm_feasible_instances` 还要求完整本地 P KV。[C15](#c15) 在线 `_plan_train` 按现有 active_decode 成员和 prefill drain 构造列车。[C16](#c16) 这些是需要接入新边界的具体位置，不据片段断言所有回调已审查。

**目标。**按第 14.5/16.4 节执行阶段事务：保护基础区间，取得新 D 目标承诺及来源引用，释放未被物理工作占用的旧默认增长承诺，成功后锁定 D；旧数据和传输按真实安全事件释放。P 边界到 D 提交期间不能误当冷 victim，旧 buffer 未空不能转用。

**修改位置。**新增阶段 reservation-transfer/resize API 及幂等 epoch；不要先删旧拒绝再依赖未经重设计的历史路径。请求运行态同时保存 P/D 实例、阶段 action 和最终完成归属。构图器不得把本请求尚未决定位置的 D 纳入 P 列车；选定后在目标处建立第一步门与后续有限构图。其他请求继续执行，不引入全系统 P/D 栅栏。

**验收。**模拟新目标容量失败、重复 P 完成通知、取消、在途 copy ack、同 tick 多请求竞争；既不能丢旧引用/预约，也不能预约双份可复用增长。没有有效的新阶段提交时原地方案仍合法；已提交物理动作后不能无声重选。统计第二次控制、首/次 token 停顿及全部真实交接，不以预测器检查代替后端验证。

## 23. 建议的模块边界：只拆必要接口

以下是建议的接口组织，不要求按名称新增一堆文件。

| 接口/数据 | 必须携带 | 不得携带 |
|---|---|---|
| `ModelWork` | 层/rank 实际尺寸、算子工作模板、chunk/tile、TP 模板 | 未来输出真值 |
| `DecisionSnapshot` | 阶段/epoch、版本、当前负载/区间布局、引用、资源与容量、旧已发义务 | 后端未来完成队列 |
| `StageWork` / `BoundaryKVLayout` | H_B、P/D 实例、基础区间与副本、pending token、继续方案、剩余前向估计 | 已完成 P 再收费、未知目标的提前传输 |
| `PhaseFootprint` | 持久 KV、临时工作区、双缓冲、可复用本地分配 | 其他实例未来释放的虚假抵扣 |
| `TransferSpec` | 真实源/目标/edge、方向、各资源工作、启动与完成门 | 只有聚合总字节却没有物理路径 |
| `EstimateResult` | 阶段、继续标记、下一 token/响应/结算预测、最终布局、成本与近似标记 | 所有未来 token 的事件数组 |
| `StageCommit` | 新旧预留差量、引用转移、继承义务、一次提交标记 | 任意取消已发操作或重复提交 |

预测器与执行器共享的是**工作量和协议说明**：字节、分块、路径、就绪/释放条件。执行器必须独立推进真实资源事件，不直接拿预测总时间作为测量真值。

## 24. 修改顺序与验收边界

| 阶段 | 必须完成 | 本阶段不能声称什么 |
|---|---|---|
| 1. 等价瘦身 | sum/count、决策内共享、压缩诊断记录；保持旧选择 | 不能声称新模型收益 |
| 2. 工作与协议统一 | 前向步数、P/D 重算 span、有限缓冲、边界/引用/预约事务、真实就绪 | 不能用只改 cost 代表新执行协议；不能仅删同址断言 |
| 3. 新估价内核 | 两阶段有限遍历、逐层就绪、池路径、HBM 共用、分段增长、容量摘要、有限来源 merge | 不能声称全局 P/D 组合最优或未测低开销 |
| 4. 受控验证 | 守恒、两个边界同状态候选、一次/两次映射对照、ITL、控制与仿真开销、内存扩展 | 不能把局部选择或首 token 后迁移收益写成全局/长期保证 |

阶段 2 的协议变化必须对共同物理能力一致实现，再进行策略比较；不能让某个对照继续使用免费缓冲或漏计写带宽，也不能把本文主动改变的协议收益全部归到代价模型选择上。

**最重要的执行边界**：这份文档是可以据以施工和验证的目标规格，但不是已修改完成的仓库状态。本次没有向 GitHub 写入文件、提交代码或启动正式仿真。

---

<a id="formula-checks"></a>

# 附录 A：公式核对与参数清单

## A.1 双缓冲尾块的可直接实现式

若有 m 个完整块和一个尾块 $(x_t,c_t)$，记 $A=\max(P,S+x)$、$z=\max(x,c)$：


$$
F_m=A+c+(m-1)z.
$$


m=1 时，尾块传输从 A 开始。m≥2 时：


$$
T_m=\begin{cases}A+(m-1)x,&x\ge c,\\A+(m-2)c+x,&x<c,\end{cases}
\quad F_{m-1}=A+c+(m-2)z,
$$


$$
F_{tail}=\max\bigl(\max(T_m,F_{m-1})+x_t,\ F_m\bigr)+c_t.
$$


没有完整块、只有尾块时，直接为 $\max(P,S+x_t)+c_t$。恰好整除时不生成零字节尾事务。全部式子使用与第 12 节相同的首块等待规则。

## A.2 固定服务时间下，停顿不能重复累计

对 $F_g=\max(F_{g-1},R_g)+c_g$，令 $D_g=\sum_{j<g}c_j$，则：


$$
F_L-\sum_{g=1}^{L}c_g=\max\left(0,\max_g(R_g-D_g)\right).
$$


这个结果只用于固定服务时间的单链验证。共享资源改变了 `c_g` 时，必须重算相应服务，而不能仍把它当作一般系统的精确式。

## A.3 本次实际做了哪些检查

本版以固定随机种子 `20260926` 重新执行独立公式检查，并新增 P→D 边界算术/身份样例：

| 检查 | 样例数 | 结果 |
|---|---:|---|
| 双缓冲完整块闭式与逐块递推 | 3,000 | 全部一致 |
| 含不满尾块的闭式与逐块递推 | 3,000 | 全部一致 |
| 固定速率、单调服务下的区段上下限 | 1,000 | 全部成立 |
| 固定服务层链的最终停顿恒等式 | 1,000 | 全部一致 |
| P→D 来源/字节、继续平局、控制差额、预留、有限位置 merge 与旧义务合成 | 24 | 全部通过 |

这些 8,024 个检查验证的是限定模型中的代数、算例身份和边界规则，不验证真实 NoC 仲裁、Transformer kernel、仓库的阶段事务或仿真加速比。新增 24 个样例不是端到端 P/D 调度器测试；该运行验证仍须在代码改造后完成。

## A.4 参数冻结表

| 参数 | 本版决定 | 解释 |
|---|---|---|
| 层级粒度 | 默认一层；真实块原子就绪门必须保留 | 不跨真实数据依赖压缩 |
| 重复段区段数 | 最大 8 | 计算预算，不是调优权重 |
| 无样本 decode 先验 | 32 个额外前向步 | 显式冷启动假设，必须标记 |
| 在线无争用效率初值 | 1 | 配置峰值冷启动，不保证真实能达到 |
| 在线效率更新 | 固定 1/16 EWMA | 非 workload 拟合 |
| remote 暂存 | 每 rank 两块 | 字节大小来自执行/硬件配置，必须真实预约 |
| prefill/query tile/传输通道数 | 静态执行配置 | 不由真实未来长度决定 |
| P/D 位置 | 初始选择 P；P 完成处允许一次 D 重选 | 不强制同址，不强制异址 |
| 初始 D 假设 | 按该 P 候选形成的布局原地继续 | 只估一种默认延续，不搜索 P/D 笛卡尔积 |
| D 继续平局 | 相同完整预测成本优先不改方案 | 不人为增加迁移奖励 |
| D 未终止的预测步数 | 至少 1 个剩余前向；已终止则跳过 D | 不用真实未来长度 |
| D 方案冻结 | 第一笔不可撤销的新阶段工作提交后 | 不在 D 中重复重映射 |
| 片上位置界 | 初始一处，P 后最多两处，D 后最多三处 | 超出布局范围时重新建模 |
| merge 目标 | 已有完整并集优先；否则最多三处中唯一保留量最大者 | 同量按边界 home、D、编号；无进展才有限容量回退 |
| 候选内完整 E 递推 | 禁止作为主路径 | 使用增量容量摘要 |
| 时间取整 | 最终等待/完成向上取整 | 零工作不凭空产生事务 |

## A.5 本版公式显示检查

本版对 37 个独立数学块和 168 个行内表达式，共 205 项执行 MathJax 本地渲染检查，未出现数学解析错误。另在 Chromium 中检查随附离线 HTML：公式均有可见高度，桌面与窄屏预览没有整页横向溢出，并抽查了阶段成本、双缓冲与合并章节。

此检查证明该文件的公式能够在上述本地渲染流程中显示，不表示每个 Markdown 软件默认开启数学支持。Markdown 使用独立双美元和行内单美元定界，公式编号位于数学块外；离线 HTML 把相同公式直接保存为 SVG，不依赖外部脚本、字体文件或联网加载。

---

<a id="sources"></a>

# 附录 B：证据与源码定位

源码证据只用于第二部分对照；不将旧代码约定当作第一部分的顶层依据。


## B.1 本次静态审查的源码片段

所有链接固定到 commit，不随 main 后续变化。下列行号是 GitHub 原文件行号，不是聊天工具返回的 JSON 包装行号；引用只证明列出的已读片段，不代表全仓审计。

**版本证据**：[本次提交](https://github.com/cs-sunH/template/commit/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d)。

<a id="c01"></a>

**C01**　[joint/joint_cost_model.py，L195—L253](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_cost_model.py#L195-L253)  
CausalHorizonEstimator 与 ServiceFactors 开头：列表均值、运行统计、因果长度、更新规则。

<a id="c02"></a>

**C02**　[joint/joint_cost_model.py，L1030—L1195](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_cost_model.py#L1030-L1195)  
传输原语、池端口、源/目标 HBM、merge 写腿条件、全路径除数。

<a id="c03"></a>

**C03**　[joint/joint_cost_model.py，L1685—L1915](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_cost_model.py#L1685-L1915)  
remote 字节与 credit、计算段、prefill 回调、固定 decode 单步费用。

<a id="c04"></a>

**C04**　[joint/joint_cost_model.py，L1940—L2150](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_cost_model.py#L1940-L2150)  
正反 merge 路由、字节方向判据、空间估计、三种最终关键路径合成。

<a id="c05"></a>

**C05**　[joint/joint_cost_model.py，L2180 起](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_cost_model.py#L2180)  
扫描次数、decode 增长、逐 rank 足迹、部分重算折算与驱逐粗估。

<a id="c06"></a>

**C06**　[joint/joint_cost_model.py，L690—L850](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_cost_model.py#L690-L850)  
TelemetryLinkFlowView：流数优先、总体速率回退、旧流与候选合成。

<a id="c07"></a>

**C07**　[joint/joint_scheduler.py，L165—L233](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/joint_scheduler.py#L165-L233)  
全部 instance × action 枚举、joint argmin 与平局规则。

<a id="c08"></a>

**C08**　[joint/event_recursion_predictor.py，L961—L1101](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/joint/event_recursion_predictor.py#L961-L1101)  
predict_k_hide 枚举、all_traces、逐层完成偏移累计。

<a id="c09"></a>

**C09**　[face_scheduler.py，L2200—L2410](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/face_scheduler.py#L2200-L2410)  
容量最终可行性接口、reserve_request_capacity、分片/后缀预约。

<a id="c10"></a>

**C10**　[face_scheduler.py，L3650—L3830](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/face_scheduler.py#L3650-L3830)  
逐出合法性、逻辑账本修改、_ensure_capacity 的 T/E 规划入口。

<a id="c11"></a>

**C11**　[online/sh30_online_scheduler.py，L2600—L2780](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/online/sh30_online_scheduler.py#L2600-L2780)  
真实流登记/注销、serial credit 去重、pool/HBM 端点登记边界。

<a id="c12"></a>

**C12**　[online/sh30_online_scheduler.py，L3830—L4025](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/online/sh30_online_scheduler.py#L3830-L4025)  
遥测接入与过期、HBM/池除数、服务样本、transfer_factor 披露身份。

<a id="c13"></a>

**C13**　[online/graph_batch_builder.py，L2500—L2700](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/online/graph_batch_builder.py#L2500-L2700)  
copy/remote/后缀恢复旁支、已有层门、恢复组并行和缓冲容量注释。

<a id="c14"></a>

**C14**　[chatgpt建议实验思路.md，L312—L367](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/chatgpt建议实验思路.md#L312-L367)  
根目录实验总纲：7.2 代价目标、现状与验证要求，部分表述仍为旧口径。


<a id="c15"></a>

**C15**　[face_scheduler.py，L2350—L2440](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/face_scheduler.py#L2350-L2440)  
本版新增核读：`move_request_capacity_reservation` 的同实例 no-op/跨实例拒绝，以及 `decode_hbm_feasible_instances` 的完整本地 P KV 前提。用于 D18—D20，不代表已审查所有执行回调。

<a id="c16"></a>

**C16**　[online/sh30_online_scheduler.py，L1070—L1200](https://github.com/cs-sunH/template/blob/304d8e8d844b1ab65bb686ffa63bdb6053c12b9d/astra-sim-joint/sh_test_mesh/workload/llama2_7b_inference/online/sh30_online_scheduler.py#L1070-L1200)  
本版新增核读：`_plan_train` 的 active_decode、prefill chunk、drain 与列车构造。用于定位新 P→D 边界需要联动的构图/调度接口，不单凭片段宣称所有分支行为。

## B.2 仅用于基本模型性质的原始资料

<a id="a1"></a>

**A1**　[GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints](https://arxiv.org/abs/2305.13245)。用于 Q 头与 KV 头并非同一数量这一基础事实；不用于为本文新代价模型背书。

<a id="a2"></a>

**A2**　[Attention Is All You Need，模型结构与 attention 部分](https://arxiv.org/html/1706.03762v7)。用于层间计算依赖及 attention 的基础算子结构；“完整重算”是本文选择的执行语义，不是声称原论文规定了本系统的调度动作。

<a id="a3"></a>

**A3**　[FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness](https://arxiv.org/abs/2205.14135)。用于分块、有限中间状态的可执行依据；具体 query/KV tile、HBM 双缓冲、8 区段和服务估计都是本设计的显式决定，不声称来自该论文。

## B.3 公式显示依据

[GitHub 官方：编写数学表达式](https://docs.github.com/zh/get-started/writing-on-github/working-with-advanced-formatting/writing-mathematical-expressions)说明 Markdown 文件支持行内 `$...$` 与独立 `$$...$$` 数学表达式。本文件不把 LaTeX 放入普通代码块，也不使用仅对部分阅读器有效的括号式数学定界符；公式编号在数学块外。纯文本模式或未启用数学插件的阅读器仍显示源码，此时使用随附离线 HTML。

---

**交付边界**：第一部分是一套明确、可检验的目标设计；第二部分给出落实它所需的代码差异。新增近似必须接受独立误差验证，新增物理协议必须同步改变执行路径。此文档不代表仓库已修改，也不代表已取得性能或 ISCA 录用结论。
